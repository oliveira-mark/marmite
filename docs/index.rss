<?xml version="1.0" encoding="utf-8"?><rss version="2.0" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>My Blog</title><link>https://www.myblog.com/blog/</link><description></description><pubDate>Tue, 26 Nov 2024 00:00:00 GMT</pubDate><lastBuildDate>Mon, 27 Jan 2025 10:50:32 GMT</lastBuildDate><generator>marmite</generator><image><url>https://www.myblog.com/blog//./media/og_image.jpg</url><title></title><link></link></image><item><title>Marmite Command Line Interface</title><link>https://www.myblog.com/blog//marmite-command-line-interface.html</link><author>rochacbruno</author><category>docs</category><guid>https://www.myblog.com/blog//marmite-command-line-interface.html</guid><pubDate>Tue, 26 Nov 2024 00:00:00 GMT</pubDate><source url="https://www.myblog.com/blog/">index</source><content:encoded><![CDATA[<p>Marmite CLI is designed to be always executed pointing to an <code>input_folder</code> where the markdown content is located,
and besides generating the site it comes with other useful features.</p>
<p>The basic usage is very simple:</p>
<pre><code class="language-console">$ marmite input_folder
Site generated at input_folder/site
</code></pre>
<p>Or you can specify the output folder:</p>
<pre><code class="language-console">$ marmite input_folder output_folder
Site generated at output_folder
</code></pre>
<blockquote>
<p>Check <code>marmite --help</code> for a complete list.</p>
</blockquote>
<p>Besides this basic usage Marmite comes with other useful arguments.</p>
<h2><a href="#initialize-a-project" aria-hidden="true" class="anchor" id="initialize-a-project"></a>Initialize a Project</h2>
<p>Marmite can gerate a site from any folder containing markdown files,
however if you need to initialize a project from the scratch, marmite
can generate the recommended structure.</p>
<pre><code class="language-console">$ marmite --init-site myblog
Config file generated: &quot;myblog/marmite.yaml&quot;
</code></pre>
<p>The site will be created with the default configuration, it is possible to <a href="#override-configuration" data-wikilink="true">Override Configuration</a> passing
each parameter e,g: <code>--name MySite</code>.</p>
<p>The generated structure looks like:</p>
<pre><code class="language-console">$ tree myblog
myblog
├── content
│   ├── media/
│   ├── 2024-11-26-welcome.md
│   ├── about.md
│   ├── _404.md
│   ├── _announce.md
│   ├── _comments.md
│   ├── _hero.md
│   ├── _markdown_footer.md
│   ├── _markdown_header.md
│   ├── _references.md
│   └── _sidebar.example.md
├── custom.css
├── custom.js
└── marmite.yaml
</code></pre>
<p>Read more on <a href="using-markdown-to-customize-layout.html" data-wikilink="true">Using Markdown to Customize Layout</a> and <a href="customizing-templates.html" data-wikilink="true">Customizing Templates</a></p>
<h2><a href="#add-new-content" aria-hidden="true" class="anchor" id="add-new-content"></a>Add new content</h2>
<p>To add new content you just need to create new markdown files on the <code>content</code> folder,
you can use your preferred tools and editors for that task.</p>
<p>Marmite provides the utility to automate creating the file via CLI, which is useful
to have the <strong>date</strong> attribute set automatically.</p>
<pre><code class="language-console">$ marmite myblog --new &quot;My first post&quot; -t &quot;post,content&quot;   
myblog/content/2024-11-26-12-34-27-my-first-post.md
</code></pre>
<p>Pass <code>-p</code> to create a page instead of a post.<br />
Pass <code>-e</code> to immediately open the file on the <code>$EDITOR</code></p>
<h2><a href="#generate-the-site" aria-hidden="true" class="anchor" id="generate-the-site"></a>Generate the site</h2>
<pre><code class="language-console">$ /marmite myblog -v
Site generated at: myblog/site/
</code></pre>
<p>Or specify a custom <code>output_folder</code>:</p>
<pre><code class="language-console">$ /marmite myblog /var/www/myblog -v
Site generated at: /var/www/myblog
</code></pre>
<blockquote>
<p>It is possible to <a href="#override-configuration" data-wikilink="true">Override Configuration</a> during site generation, for example, passing <code>--pagination 5</code> to change how many posts to list per page.</p>
</blockquote>
<h3><a href="#rebuild-when-content-changes" aria-hidden="true" class="anchor" id="rebuild-when-content-changes"></a>Rebuild when content changes</h3>
<p>Marmite can watch for changes on your project and rebuild the site automatically,
it is required to inform the output folder for this functionality.</p>
<p>Use <code>-w</code> or <code>--watch</code></p>
<pre><code class="language-console">$ /marmite myblog /var/www/myblog -w
Site generated at: /var/www/myblog
Watching for changed on: myblog/
</code></pre>
<h3><a href="#serving-the-site" aria-hidden="true" class="anchor" id="serving-the-site"></a>Serving the site</h3>
<p>Marmite generates a flat site, which means you can open it directly on your browser (with some limitations)
or use any web server to serve it.</p>
<p>Marmite comes with a built-in server to use only locally.</p>
<p>Use <code>--serve</code> to start the server.</p>
<pre><code class="language-console">$ /marmite myblog /var/www/myblog -w
Site generated at: /var/www/myblog
Watching for changed on: myblog/
Starting built-in HTTP server...
Server started at http://0.0.0.0:8000/ - Type ^C to stop.
</code></pre>
<p>If you want to change the address use <code>--bind ip:port</code></p>
<blockquote>
<p>[!IMPORTANT]<br />
The built-in server is not suitable for production, when deploying use a webserver such as <a href="https://nginx.org">Nginx</a> or read the <a href="hosting.html" data-wikilink="true">hosting</a> guide to learn how to deploy to Github pages, Gitlab pages, Codeberg Pages, Netlify and more.</p>
</blockquote>
<h2><a href="#start-a-new-theme" aria-hidden="true" class="anchor" id="start-a-new-theme"></a>Start a new theme</h2>
<p><strong>Marmite</strong> comes with a default theme that is optimized for readabily on blogs,
you can read more on <a href="why-to-use-marmite.html" data-wikilink="true">Why to use marmite</a> article.</p>
<p>The built-n theme comes with various colorschemes and you can set the desired colorscheme
on the <code>marmite.yaml</code> file or passing <code>--colorscheme</code> on the CLI.</p>
<p>The built-in theme can be customized using the <code>custom.css</code> placed on your root folder,
and individual templates can be overriden on a <code>templates/</code> folder.</p>
<p>Read more on <a href="customizing-templates.html" data-wikilink="true">Customizing Templates</a></p>
<p>However, if you want to start a new theme to fully customize you can use the <code>--start-theme</code> argument.</p>
<pre><code class="language-console">$ marmite myblog /tmp/mysite --start-theme 
Generated myblog/templates/*
Generated myblog/static/*
</code></pre>
<p>Now you can freely edit the static files and templates.</p>
<h2><a href="#generate-configuration" aria-hidden="true" class="anchor" id="generate-configuration"></a>Generate configuration</h2>
<h3><a href="#default" aria-hidden="true" class="anchor" id="default"></a>Default</h3>
<p>To generate default config file</p>
<pre><code class="language-console">$ marmite myblog --generate-config
Config file generated: &quot;myblog/marmite.yaml&quot;
</code></pre>
<h3><a href="#override-configuration" aria-hidden="true" class="anchor" id="override-configuration"></a>Override Configuration</h3>
<p>To override specific keys</p>
<pre><code class="language-console">$ marmite myblog --generate-config \
  --name MySite \
  --tagline &quot;My articles&quot; \
  --pagination 20 \
  --colorscheme gruvbox \
  --url &quot;https://myblog.com&quot;

Config file generated: &quot;myblog/marmite.yaml&quot;
</code></pre>
<h2><a href="#cli-help" aria-hidden="true" class="anchor" id="cli-help"></a>CLI Help</h2>
<pre><code class="language-console">$ marmite --help
Marmite is the easiest static site generator.

Usage: marmite [OPTIONS] &lt;INPUT_FOLDER&gt; [OUTPUT_FOLDER]

Arguments:
  &lt;INPUT_FOLDER&gt;   Input folder containing markdown files
  [OUTPUT_FOLDER]  Output folder to generate the site [default: `input_folder/site`]

Options:
  -v, --verbose...
          Verbosity level (0-4) [default: 0 warn] options: -v: info,-vv: debug,-vvv: trace,-vvvv: trace
          all
  -w, --watch
          Detect changes and rebuild the site automatically
      --serve
          Serve the site with a built-in HTTP server
      --bind &lt;BIND&gt;
          Address to bind the server [default: 0.0.0.0:8000]
  -c, --config &lt;CONFIG&gt;
          Path to custom configuration file [default: marmite.yaml]
      --init-templates
          Initialize templates in the project
      --start-theme
          Initialize a theme with templates and static assets
      --generate-config
          Generate the configuration file
      --init-site
          Init a new site with sample content and default configuration this will overwrite existing files
          usually you don't need to run this because Marmite can generate a site from any folder with
          markdown files
      --new &lt;NEW&gt;
          Create a new post with the given title and open in the default editor
  -e
          Edit the file in the default editor
  -p
          Set the new content as a page
  -t &lt;TAGS&gt;
          Set the tags for the new content tags are comma separated
      --name &lt;NAME&gt;
          Site name [default: &quot;Home&quot; or value from config file]
      --tagline &lt;TAGLINE&gt;
          Site tagline [default: empty or value from config file]
      --url &lt;URL&gt;
          Site url [default: empty or value from config file]
      --footer &lt;FOOTER&gt;
          Site footer [default: from '_footer.md' or config file]
      --language &lt;LANGUAGE&gt;
          Site main language 2 letter code [default: &quot;en&quot; or value from config file]
      --pagination &lt;PAGINATION&gt;
          Number of posts per page [default: 10 or value from config file]
      --enable-search &lt;ENABLE_SEARCH&gt;
          Enable search [default: false or from config file] [possible values: true, false]
      --content-path &lt;CONTENT_PATH&gt;
          Path for content subfolder [default: &quot;content&quot; or value from config file] this is the folder
          where markdown files are stored inside `input_folder` no need to change this if your markdown
          files are in `input_folder` directly
      --templates-path &lt;TEMPLATES_PATH&gt;
          Path for templates subfolder [default: &quot;templates&quot; or value from config file]
      --static-path &lt;STATIC_PATH&gt;
          Path for static subfolder [default: &quot;static&quot; or value from config file]
      --media-path &lt;MEDIA_PATH&gt;
          Path for media subfolder [default: &quot;media&quot; or value from config file] this path is relative to
          the folder where your content files are
      --default-date-format &lt;DEFAULT_DATE_FORMAT&gt;
          Default date format [default: &quot;%b %e, %Y&quot; or from config file] see
          &lt;https://docs.rs/chrono/0.4.19/chrono/format/strftime/index.html&gt;
  -h, --help
          Print help
  -V, --version
          Print version
</code></pre>
<blockquote>
<p>Please consider giving a ☆ on Marmite <a href="https://github.com/rochacbruno/marmite">Github</a> repository, that helps a lot!</p>
</blockquote>
<!-- Contents from _markdown_footer.md are appended to every content -->
<!-- _references.md is a file to gather all references together 
this file is appended to every raw markdown.
-->
]]></content:encoded></item><item><title>Getting started</title><link>https://www.myblog.com/blog//getting-started.html</link><author>rochacbruno</author><category>docs</category><guid>https://www.myblog.com/blog//getting-started.html</guid><pubDate>Thu, 24 Oct 2024 00:00:00 GMT</pubDate><source url="https://www.myblog.com/blog/">index</source><content:encoded><![CDATA[<p>Learn how to create your blog with Marmite in minutes, you can start with zero-config  and then customize gradually later.</p>
<p>Not convinced yet? Read <a href="why-to-use-marmite.html" data-wikilink="true">why-to-use-marmite</a></p>
<h2><a href="#quick-start" aria-hidden="true" class="anchor" id="quick-start"></a>Quick Start</h2>
<h3><a href="#installation" aria-hidden="true" class="anchor" id="installation"></a>Installation</h3>
<p>Marmite is written in Rust 🦀 so if you have Rust in your system you
can use cargo to install it.</p>
<pre><code class="language-bash">cargo binstall marmite
</code></pre>
<p>or</p>
<pre><code class="language-bash">cargo install marmite
</code></pre>
<p>Or download the pre-built <strong>binary</strong> from the <a href="https://github.com/rochacbruno/marmite/releases">releases</a></p>
<details>
<summary>Or use docker</summary>
<blockquote>
<p>[!IMPORTANT]
The directory containing your marmite project must be mapped to containers <code>/input</code><br />
If running inside the directory use <code>$PWD:/input</code>
The result will be generates in a <code>site</code> folder inside the input dir.</p>
</blockquote>
<p>Build</p>
<pre><code class="language-console">$ docker run -v $PWD:/input ghcr.io/rochacbruno/marmite
Site generated at: site/
</code></pre>
<p>Serve (just add port mapping and the --serve)</p>
<pre><code class="language-console">$ docker run -p 8000:8000 -v $PWD:/input ghcr.io/rochacbruno/marmite --serve
</code></pre>
</details>
<h3><a href="#starting-your-site-project" aria-hidden="true" class="anchor" id="starting-your-site-project"></a>Starting your site project</h3>
<p>For a simple website marmite doesn't require any specific directory structure,
you just need a directory containing markdown files.</p>
<p>So start by creating a new directory for your content.</p>
<pre><code class="language-bash">mkdir myblog
</code></pre>
<h3><a href="#adding-content" aria-hidden="true" class="anchor" id="adding-content"></a>Adding content</h3>
<p>On this example lets add an <strong>about</strong> page:</p>
<p><code>myblog/about.md</code></p>
<pre><code class="language-markdown"># About me

I am a person that loves simplicity and writing content in 
Markdown format.

Email me at &lt;me@example.com&gt;
Follow me on &lt;https://social.example.com/@me&gt;
</code></pre>
<p>And a <strong>hello world</strong> post:</p>
<p><code>myblog/hello-world.md</code></p>
<pre><code class="language-markdown">---
date: 2024-10-25
tags: hello,world
---
# Hello World

Hello, this is my first post on my new blog generated
by Marmite[1], a very simple to use blog generator.

I can write full CommonMark and GFM Markdown.

Emojis :smile:

[1]: https://github.com/rochacbruno/marmite
</code></pre>
<p>The main difference from those 2 files is the fact that the first one <strong>about.md</strong>
doesn't have a <code>date:</code> metadata and since marmite can't detect its date, it considers
it a <strong>page</strong>.</p>
<p>The second, <strong>hello-world.md</strong> has a <code>date: ...</code> on its <strong>FrontMatter</strong> so
marmite considers it a <strong>chronological</strong> content, a <strong>post</strong> and will list it
on your blog front-page.</p>
<p>Learn more about <a href="content-types.html" data-wikilink="true">Content Types</a></p>
<h3><a href="#using-the-cli" aria-hidden="true" class="anchor" id="using-the-cli"></a>Using the CLI</h3>
<p><strong>Alternatively</strong> you can ask marmite to create the initial structute and config for you.</p>
<pre><code class="language-console">$ marmite myblog --init-site \
  --name Mysite \
  --tagline &quot;My Articles and Notes&quot; \
  --colorscheme nord \
  --toc true \
  --enable-search true
</code></pre>
<p>And content using</p>
<pre><code class="language-console">$ marmite myblog --new &quot;This is my new post title&quot; -t &quot;post,notes&quot; -e
</code></pre>
<h3><a href="#generating-the-site" aria-hidden="true" class="anchor" id="generating-the-site"></a>Generating the site</h3>
<blockquote>
<p>I'm a big user of other SSGs but it is frequently frustrating that it takes so much setup to get started.<br />
Just having a directory of markdown files and running a single command sounds really useful.<br />
— Michael, marmite user.</p>
</blockquote>
<p>As said above, you just need to run marmite and point your content directory,
the usage is <code>marmite [input_folder] [output_folder] [options]</code></p>
<p>So do:</p>
<pre><code class="language-console">$ marmite myblog site -v

INFO Config loaded from: defaults
INFO Generated site/index.html
INFO Generated site/pages.html
INFO Generated site/hello-world.html
INFO Generated site/about.html
INFO Generated site/404.html
INFO Generated site/tag-hello.html
INFO Generated site/tag-world.html
INFO Generated site/tags.html
INFO Generated site/static/*{css,js,fonts}
INFO Generated site/index.rss
INFO Generated site/{tag-*}.rss
INFO Generated site/{author-*}.rss
INFO Site generated at: site/
</code></pre>
<blockquote>
<p>[!TIP]
You can omit the INFO messaged by omiting the <code>-v</code> or increase by adding up to 4 <code>-vvvv</code></p>
</blockquote>
<p>And that's all! you have full working blog on the <code>site</code> folder, as <strong>marmite</strong>
generates a <strong>flat html</strong> website, you can open directly in your browser.</p>
<p>To see your site you can open the <code>index.html</code> in your browser, type <code>Ctrl + O</code>
on your keyboard and your browser will let you navigate to the folder and then
select the <code>index.html</code> file.</p>
<figure>
  <figcaption>Ctrl + O</figcaption>
  <img src="./media/screenshots/browser_open.png" width="300">
</figure>
<p>Then your site will look like:</p>
<figure>
  <figcaption>Home Page</figcaption>
  <img src="./media/screenshots/index.png" width="600">
</figure>
<figure>
  <figcaption>About Page</figcaption>
  <img src="./media/screenshots/about.png" width="600">
</figure>
<figure>
  <figcaption>Post Light Mode</figcaption>
  <img src="./media/screenshots/hello-world.png" width="600">
</figure>
<figure>
  <figcaption>Post Dark Mode</figcaption>
  <img src="./media/screenshots/hello-world-dark.png" width="600">
</figure>
<figure>
  <figcaption>Tags</figcaption>
  <img src="./media/screenshots/tags.png" width="600">
</figure>
<h3><a href="#automatic-re-generation-when-files-changes" aria-hidden="true" class="anchor" id="automatic-re-generation-when-files-changes"></a>Automatic re-generation when files changes</h3>
<p>Instead of manually executing the command every time you change your
markdown files you can tell marmite to detect the changes and regenerate.</p>
<pre><code>marmite myblog site --watch
...
Watching for changes in folder: myblog
</code></pre>
<h3><a href="#serving-the-site" aria-hidden="true" class="anchor" id="serving-the-site"></a>Serving the site</h3>
<p>Marmite comes with a built-in server, this server is not meant to use in
production, when publishing your site you are probably going to use a
webserver such as <strong>Apache</strong> or <strong>Nginx</strong>, or most probably use a free
static hosting service such as <strong>Github pages</strong>, <strong>Netlify</strong> or <strong>CLoudflare</strong>.</p>
<p>However, during the content writing you want to check how the website looks,
so the built-in server comes handy, just add <code>--serve</code></p>
<pre><code>marmite myblog site --watch --serve
...
Watching for changes in folder: myblog
Starting built-in HTTP server
Server started at http://localhost:8000/ - Type ^C to stop.
</code></pre>
<p>Open your browser and check <a href="http://localhost:8000/">http://localhost:8000/</a> to see your running blog</p>
<p>if you want to share your site with others in the same network, just
pass <code>--bind &quot;0.0.0.0:8000</code> and then share your local IP address.</p>
<h2><a href="#media" aria-hidden="true" class="anchor" id="media"></a>Media</h2>
<p>You gonna want some nice images in your blog posts, it is easy and simple
to add it with marmite.</p>
<p>Create your <code>myblog/media</code> folder and include any image or video you want inside it
then in your markdown content just reference using the relative path.</p>
<p><code>myblog/hello-world.md</code></p>
<pre><code class="language-markdown">---
date: 2024-10-25
tags: hello,world
---
# Hello World

This is my photo:
![me](media/myphoto.png)
</code></pre>
<figure>
  <figcaption>Media</figcaption>
  <img src="./media/screenshots/media.png" width="600">
</figure>
<blockquote>
<p>[!NOTE]<br />
You <strong>can create</strong> subfolders in the media directory, that is useful to organize
files by topic or post.</p>
</blockquote>
<h2><a href="#organizing-content" aria-hidden="true" class="anchor" id="organizing-content"></a>Organizing content</h2>
<p>For now all your markdown files are located on <code>myblog/</code> root folder,
that is enough if you are going to stick with the defaults, but as soon you
start customizing templates and configuration you gonna need to organize your
contents.</p>
<p><strong>marmite</strong> generated a <strong>flat html</strong> site, that means you can't have subpaths,
your content will always be served from the top level.</p>
<p>But for better organization of your project <strong>YOU CAN OPTIONALLY</strong> move all your <strong>markdown</strong>
files to a subfolder called <code>content/</code></p>
<pre><code class="language-bash">myblog/
   |_ content/
         |_ about.md
         |_ hello-world.md
         |_ media/
             |_ myphoto.png
</code></pre>
<h2><a href="#configuring" aria-hidden="true" class="anchor" id="configuring"></a>Configuring</h2>
<p>marmite is designed to be <strong>zero</strong> config to get started, just like you
did above!</p>
<p>But you probably want to customize couple of things in your website to give
your personal touch, change the look and feel, or even completely customize
its front-end if you know some <strong>CSS/JS</strong> magic.</p>
<h3><a href="#adding-your-blog-information" aria-hidden="true" class="anchor" id="adding-your-blog-information"></a>Adding your blog information</h3>
<p>If found, marmite will load configuration from a file named <code>marmite.yaml</code>
located in the root of your input_folder, in this case:</p>
<p><code>myblog/marmite.yaml</code></p>
<pre><code class="language-yaml">name: My Personal Blog
tagline: Things I like to share
pagination: 10
menu:
  - [&quot;about&quot;, &quot;about.html&quot;]
  - [&quot;pages&quot;, &quot;pages.html&quot;]
  - [&quot;tags&quot;, &quot;tags.html&quot;]
  - [&quot;archive&quot;, &quot;archive.html&quot;]
</code></pre>
<p>Now regenerate your site with</p>
<pre><code>marmite myblog site --watch --serve
</code></pre>
<p>And refresh your browser to take a look at the customizations.</p>
<figure>
  <figcaption>Config</figcaption>
  <img src="./media/screenshots/config.png" width="600">
</figure>
<p>You can see all available options on the Marmite <a href="https://github.com/rochacbruno/marmite/blob/main/example/marmite.yaml" title="Config Example">example config</a></p>
<p>You can generate a config file with the default values for editing:</p>
<pre><code class="language-console">$ marmite myblog site --generate-config
Config generated at `myblog/marmite.yaml`
</code></pre>
<h4><a href="#configuring-via-cli" aria-hidden="true" class="anchor" id="configuring-via-cli"></a>Configuring via CLI</h4>
<p>You can also pass configuration variables directly to the CLI when building</p>
<pre><code class="language-console">$ marmite myblog site \
  --name MySite \
  --tagline &quot;My articles and notes&quot; \
  --url &quot;https://myblog.com&quot; \
  --pagination 20 \
  --toc true \
  --enable-search true \
  --colorscheme dracula \
  --language en \
  --footer &quot;- &lt;strong&gt;CopyLeft&lt;/strong&gt; -&quot;
</code></pre>
<h3><a href="#enabling-comments" aria-hidden="true" class="anchor" id="enabling-comments"></a>Enabling Comments</h3>
<p>Most of blogs will have a comment box to receive feedback from readers,
marmite doesn't come with one as it is a static site and doesn't have the
ability to handle dynamic data, instead, marmite allows you to plug external
commenting system providers such as <strong>disqus</strong> or <strong>github</strong>.</p>
<p><code>myblog/_comments.md</code></p>
<pre><code class="language-yaml">##### Comments

&lt;div id=&quot;#comment-box&quot;&gt;&lt;/div&gt;
&lt;script&gt; ... &lt;/script&gt;
</code></pre>
<p>The content depends on which commenting system you choose and how it is configured.</p>
<p>Read more on <a href="./enabling-comments.html" title="Enabling Comments">Enabling Comments</a> page to learn how to enable <strong>Gisqus</strong>, a
commenting system based on Github discussions.</p>
<h2><a href="#enabling-search" aria-hidden="true" class="anchor" id="enabling-search"></a>Enabling Search</h2>
<p>Marmite can generate static search index as a <code>JSON</code> file
and then using Javascript library <a href="https://fusejs.io" title="Fuse JS">Fuse</a> provide full text search for posts and pages.</p>
<p>To enable this feature add to your <code>marmite.yaml</code></p>
<pre><code class="language-yaml">enable_search: true
search_title: Search
</code></pre>
<h2><a href="#special-pages-and-fragments" aria-hidden="true" class="anchor" id="special-pages-and-fragments"></a>Special pages and Fragments</h2>
<p>There are some contents that are considered <strong>special</strong> as those are
not regular posts or pages, right now there are:</p>
<h3><a href="#global-fragments" aria-hidden="true" class="anchor" id="global-fragments"></a>Global fragments</h3>
<p>The Global fragments are written in <strong>dynamic</strong> markdown format and allows raw html
and also allows <a href="https://keats.github.io/tera/docs/#templates" title="Tera Docs">Tera</a> expressions as those are rendered with global context.</p>
<details>
<summary>Click here to see an example.</summary> 
<p><strong>full markdown</strong></p>
<pre><code class="language-html">##### tags

{% for name, items in group(kind='tag') -%}
- [{{name}}](tag-{{name | slugify}}.html)
    {% for item in items -%}
    - [{{item.title}}]({{item.slug}}.html)
    {% endfor %}
{% endfor %}
</code></pre>
<p><strong>mixed html</strong></p>
<pre><code class="language-html"># Hello

Markdown **here**

&lt;!-- Mind the blank space between markdown and HTML blocks --&gt;
&lt;h3&gt;Tags&lt;/h3&gt;
&lt;ul&gt;
{%for tag, items in group(kind='tag') -%}
&lt;li&gt;
    &lt;a href=&quot;tag-{{tag}}.html&quot;&gt;{{tag}}&lt;/a&gt;
    &lt;ul&gt;
        {% for item in items %}
        &lt;li&gt;&lt;a href=&quot;{{item.slug}}.html&quot;&gt;{{item.title}}&lt;/li&gt;
        {% endfor %}
    &lt;/ul&gt;
&lt;/li&gt;  
{%- endfor %}
&lt;/ul&gt;
</code></pre>
</details>
<dl>
<dt>Hero - <code>_hero.md</code></dt>
<dd>
<p>A banner that shows as the first content in your home page<br />
Marmite will look for a file named <code>_hero.md</code> within your content folder.</p>
</dd>
<dt>404 - <code>_404.md</code></dt>
<dd>
<p>The page that will show for Not Found error<br />
Marmite will look for a file named <code>_404.md</code> within your content folder.<br />
if not found, marmite will generate a default.</p>
</dd>
<dt>Footer - <code>_footer.md</code></dt>
<dd>
<p>The content of <code>_footer.md</code> will be shown on the footer of base template
this will override the contents of <code>marmite.yaml</code> <code>footer:</code> field</p>
</dd>
<dt>Header - <code>_header.md</code></dt>
<dd>
<p>The content of <code>_header.md</code> will be shown on the header of base template
this will replace the Logo, Name, Tagline and menu entirely.</p>
</dd>
<dt>Comments - <code>_comments.md</code></dt>
<dd>
<p>The content of <code>_comments.md</code> will be rendered at the end of each post page,
If exits this will have precedence over the <code>extra.comments</code> on <code>marmite.yaml</code></p>
</dd>
<dt>Announce - <code>_announce.md</code></dt>
<dd>
<p>The content of <code>_announce.md</code> will be rendered on the very top of the website, globally
as a way to add alerts, annoucements and callouts.</p>
</dd>
</dl>
<h3><a href="#content-fragments" aria-hidden="true" class="anchor" id="content-fragments"></a>Content Fragments</h3>
<p>The content fragments are <strong>static</strong> Markdown format and allows raw HTML.</p>
<blockquote>
<p>[!IMPORTANT]<br />
<a href="https://keats.github.io/tera/docs/#templates" title="Tera Docs">Tera</a> expressions are not rendered on these fragments because those are rendered
before the global context is built.</p>
</blockquote>
<dl>
<dt>References - <code>_references.md</code></dt>
<dd>
<p>This file will be appended to the end of every markdown file
processed, so it is possible to use the references and footnotes
globally.
<strong>format</strong></p>
<pre><code class="language-markdown">[name]: https://link
</code></pre>
</dd>
<dt>Markdown Footer - <code>_markdown_footer.md</code></dt>
<dd>
<p>This file will be appended to the end of every markdown processed
You can include anything inside it, this is useful to add custom
callouts, messages, signatures to the end of contents.</p>
</dd>
<dt>Markdown Header - <code>_markdown_header.md</code></dt>
<dd>
<p>This file will be prepended to the top of every markdown processed
You can include anything inside it, this is useful to add custom
callouts, messages, signatures to the end of contents.</p>
</dd>
</dl>
<figure>
  <figcaption>Hero Section Enabled</figcaption>
  <img src="./media/screenshots/hero.png" width="600">
</figure>
<h2><a href="#colorschemes" aria-hidden="true" class="anchor" id="colorschemes"></a>Colorschemes</h2>
<p>Marmite comes with some colorschemes built-in, colorschemes are CSS style files that customizes colors, spacing etc.</p>
<p>if you use other applications such
as text editors, terminals and web apps you probably are familiarized with the colorschemes available.</p>
<p>To choose a colorscheme add to <code>marmite.yaml</code></p>
<pre><code class="language-yaml">extra:
  colorscheme: gruvbox
</code></pre>
<p>The built-in options are <strong>catppuccin</strong>, <strong>clean</strong>, <strong>dracula</strong>, <strong>github</strong>, <strong>gruvbox</strong>, <strong>iceberg</strong>, <strong>monokai</strong>, <strong>nord</strong>, <strong>one</strong>, <strong>solarized</strong>, <strong>typewriter</strong>.</p>
<p>Click to select a colorscheme: <select name="colorscheme" class="colorscheme-toggle"><option value="default">default</option></select><span class="theme-toggle secondary" title="dark mode">☽</span></p>
<p>To create a custom colorscheme drop a <code>custom.css</code> on your input folder (the same where marmite.yaml is located)</p>
<details>
<summary> CLICK HERE to see an example colorscheme on <code>custom.css</code> </summary>
<p><code>custom.css</code></p>
<pre><code class="language-css">/* Marmite Nord Theme */
/* picocss.com  */
:root {
    --pico-border-radius: 0;
}

.content-tags a:where(a:not([role=button])),
[role=link] {
    --pico-color: revert;
}

[data-theme=light],
:root:not([data-theme=dark]) {
    --pico-background-color: #ECEFF4;
    --pico-card-background-color: #E5E9F0;
    --pico-card-sectioning-background-color: var(--pico-background-color);
    --pico-primary: #5E81AC;
    --pico-primary-hover: #81A1C1;
    --pico-color: #2E3440;
    --pico-tag: #4C566A;
    --pico-h1-color: var(--pico-primary);
    --pico-code-background-color: var(--pico-background-color);
    --pico-table-border-color: var(--pico-card-background-color);
    --pico-color-azure-550: var(--pico-primary);
}

[data-theme=light] pre:has(&gt; code.language-mermaid) {
    background-color: var(--pico-card-background-color);
}

[data-theme=dark],
:root:not([data-theme=light]) {
    --pico-background-color: #2E3440;
    --pico-card-background-color: #3B4252;
    --pico-card-sectioning-background-color: var(--pico-background-color);
    --pico-primary: #81A1C1;
    --pico-primary-hover: #88C0D0;
    --pico-color: #D8DEE9;
    --pico-tag: #4C566A;
    --pico-h1-color: var(--pico-color);
    --pico-code-background-color: var(--pico-background-color);
    --pico-table-border-color: var(--pico-card-background-color);
    --pico-color-azure-550: var(--pico-primary);
}

[data-theme=dark] pre:has(&gt; code.language-mermaid) {
    background-color: var(--pico-code-color);
}
</code></pre>
<blockquote>
<p>[!NOTE]<br />
Multiple colorschemes can also be added to <code>static/colorschemes/{name}.css</code>
and then enable <code>extra.colorscheme_toggle</code> on config.</p>
</blockquote>
</details>
<hr>
<h2><a href="#layout-customization" aria-hidden="true" class="anchor" id="layout-customization"></a>Layout Customization</h2>
<p>If you want to keep using the default theme but wants to customize little parts
of its <code>CSS</code> and add additional <code>JS</code> then you can do it with the <strong>easy mode</strong></p>
<h3><a href="#easy-mode" aria-hidden="true" class="anchor" id="easy-mode"></a>Easy mode</h3>
<h4><a href="#custom-css" aria-hidden="true" class="anchor" id="custom-css"></a>Custom CSS</h4>
<p>Just create a new file <code>myblog/custom.css</code> and put any CSS you want inside it,
if you are customizing the default template take a look at <a href="https://picocss.com" title="Pico CSS">PicoCSS</a> docs.</p>
<p>Example:
<code>myblog/custom.css</code></p>
<pre><code class="language-css">body {
  background-color: red;
}
</code></pre>
<figure>
  <figcaption>Custom CSS</figcaption>
  <img src="./media/screenshots/custom.png" width="600">
</figure>
<h4><a href="#customizing-content-width" aria-hidden="true" class="anchor" id="customizing-content-width"></a>Customizing Content Width</h4>
<p>The content container is optimized for blogs, if you want to change the
width just add to the <code>custom.css</code></p>
<pre><code class="language-css">:root {
   --pico-container-max-width: 100ch;
}
</code></pre>
<h4><a href="#custom-js" aria-hidden="true" class="anchor" id="custom-js"></a>Custom JS</h4>
<p>The same as above, just create a <code>custom.js</code> and add any Javascript,
notice that the <code>custom.js</code> file is loaded at the bottom of the html files.</p>
<p><code>myblog/custom.js</code></p>
<pre><code class="language-javascript">console.log(&quot;Hello from marmite&quot;);
</code></pre>
<h3><a href="#advanced-mode" aria-hidden="true" class="anchor" id="advanced-mode"></a>Advanced mode</h3>
<p>Choose this option if your intention is to <strong>fully</strong> customize the default theme
or <strong>start</strong> a new theme from <strong>scratch</strong>.</p>
<h4><a href="#custom-templates" aria-hidden="true" class="anchor" id="custom-templates"></a>Custom templates</h4>
<p>To fully customize the templates you need a <code>templates</code> folder and add all the
required templates as described on <a href="./customizing-templates.html" title="Customizing Templates">Customizing Templates</a></p>
<p>If you don't want to create the templates manually, you can dump the
embedded templates in your project folder.</p>
<pre><code class="language-bash">marmite myblog site --init-templates
</code></pre>
<p>The above command will create a <code>myblog/templates</code> folder with all the required
templates and you can freely customize.</p>
<p>The templates are written in <a href="https://keats.github.io/tera/docs/#templates" title="Tera Docs">Tera</a>.</p>
<h4><a href="#custom-theme" aria-hidden="true" class="anchor" id="custom-theme"></a>Custom theme</h4>
<p>Theme is generally the templates + Style, so in addition to custom templates
you need all the static files.</p>
<p>If you wrote your templates from <strong>scratch</strong> then you are free to refer
to any static file you want, create a folder named <code>static</code> and put any
<code>css</code>, <code>js</code>, <code>fonts</code> etc inside it.</p>
<blockquote>
<p>[!IMPORTANT]
marmite generates <strong>flat html</strong> website, all static file url
will be relative like <code>./static/style.css</code> <code>./static/fonts/font.woof</code> etc.</p>
</blockquote>
<p>If you don't want to create all static files by hand, but want to reuse the
embedded theme, you can run.</p>
<pre><code class="language-bash">marmite myblog site --start-theme
</code></pre>
<p>The above command will output the <code>myblog/static</code> folder and then you can
freely customize.</p>
<h2><a href="#project-structure" aria-hidden="true" class="anchor" id="project-structure"></a>Project structure</h2>
<p>With contents and a full customized theme your repository will look like:</p>
<pre><code class="language-bash">myblog/
   |_ content/                     # Markdown content
   |     |_ _404.md
   |     |_ _hero.md
   |     |_ about.md
   |     |_ hello-world.md
   |     |_ media/                 # Images, Videos, PDFs
   |         |_ myphoto.png
   |_ templates/                   # Tera HTML templates
   |     |_ *.html
   |_ static/                      # Theme static files
   |     |_ *.{css,js,ttf,ico}
   |_ marmite.yaml                 # Config
   |_ custom.css                   # Optional css
   |_ custom.js                    # Optional JS
</code></pre>
<h2><a href="#looking-for-help" aria-hidden="true" class="anchor" id="looking-for-help"></a>Looking for help</h2>
<p>You can ask <strong>Marmite</strong> related questions or suggest features on <a href="https://github.com/rochacbruno/marmite/discussions" title="Github Discussions">Discussions</a> page</p>
<blockquote>
<p>[!NOTE]<br />
<strong>We</strong> would love to know if you publish a site  made with marmite or
created a custom theme, please share on the <a href="https://github.com/rochacbruno/marmite/discussions" title="Github Discussions">Discussions</a> page.</p>
</blockquote>
<hr />
<blockquote>
<p>Please consider giving a ☆ on Marmite <a href="https://github.com/rochacbruno/marmite">Github</a> repository, that helps a lot!</p>
</blockquote>
<!-- Contents from _markdown_footer.md are appended to every content -->
<!-- _references.md is a file to gather all references together 
this file is appended to every raw markdown.
-->
]]></content:encoded></item><item><title>Why to use Marmite?</title><link>https://www.myblog.com/blog//why-to-use-marmite.html</link><author>rochacbruno</author><category>docs</category><guid>https://www.myblog.com/blog//why-to-use-marmite.html</guid><pubDate>Mon, 21 Oct 2024 00:00:00 GMT</pubDate><source url="https://www.myblog.com/blog/">index</source><content:encoded><![CDATA[<p><strong>Marmite</strong> is a simple, easy and <strong>opinionated</strong> static site generator.</p>
<hr />
<p><strong>Marmite</strong> doesn't require specific folder structure or complex configuration,
the goal is that a <strong>blog</strong> can be generated simply by running Marmite on a folder with Markdown and media files.</p>
<p><strong>Marmite</strong> is optimized for blogs, not only the content parsing and organization features but also the built-in theme is optimized for blogging and readability.</p>
<p><strong>Marmite</strong> can also be used to create other kinds of sites as it is fully configurable and very easy to customize the templates.</p>
<p><strong>Marmite</strong> comes with a built-n theme that is very flexible, optimized for readability and with 11 colorschemes to switch.</p>
<p><strong>Marmite</strong> generated RSS and JSON feeds for all the listings in your site,
so you get feeds for main index, tags, authors and custom streams.</p>
<p><strong>Marmite</strong> aims to be easier to integrate with the social web, by adopting semantic
federation standards and integrating with the fediverse.</p>
<p><strong>Marmite</strong> is written in <strong>Rust</strong> so it is very fast and everything is included
in a single binary.</p>
<hr />
<p>To generate your static site the only thing you need is a folder with some
markdown files and <code>marmite</code>.</p>
<p>assuming you have a folder called <code>mycontent</code> containing files with <code>.md</code> extension:</p>
<pre><code class="language-plain">mycontent/
  |_ about.md
  |_ first-post.md
  |_ something-amazing-that-i-want-to-share.md
</code></pre>
<pre><code class="language-console">$ marmite mycontent mysite -v
Site generated at: /mysite
</code></pre>
<p>That is all you need to have a blog generated, now you just need to take the
<code>mysite</code> folder and publish it in a webserver, read more on <a href="./hosting.html">hosting</a>.</p>
<p>Learn how to create your first blog with marmite on our <a href="getting-started.html">Getting Started Tutorial</a></p>
<hr />
<h2><a href="#marmite-opinions" aria-hidden="true" class="anchor" id="marmite-opinions"></a>Marmite Opinions</h2>
<h3><a href="#built-in-theme" aria-hidden="true" class="anchor" id="built-in-theme"></a>Built-in Theme</h3>
<p>Marmite built-in theme has a very simple structure, it is not bloated with elements so it makes it easier to customize for your needs using only CSS variables.</p>
<p>The main goal is that you can start publishing your texts using the built-in theme and gradually customize the look and feel for your taste, without the need to spend too much time on it, focusing on writing.</p>
<p>But if you want, as the templates are simple it is also straightforward to fully customize or start from scratch.</p>
<h4><a href="#content-card-width" aria-hidden="true" class="anchor" id="content-card-width"></a>Content Card Width</h4>
<p>Baymard Institute conducted a <a href="https://baymard.com/blog/line-length-readability">research for optimal line lenght</a>, considering Marmite is optimized for blogs the built-in theme adopts by default the maximum of <code>74ch</code> width.</p>
<p><figure><img src="media/line_len.avif" alt="len" /></figure></p>
<h4><a href="#readable-font" aria-hidden="true" class="anchor" id="readable-font"></a>Readable Font</h4>
<p>The font face choosen for the built-in theme also was taken because of readability, the Braile Institute created a freefont with unique features to
improve readability in any media, that is why Marmite uses <a href="https://www.brailleinstitute.org/freefont/">Atkinson Hyperlegible</a> by default.</p>
<p><figure><img src="media/braile.png" alt="braile" /></figure></p>
<h4><a href="#design-system" aria-hidden="true" class="anchor" id="design-system"></a>Design System</h4>
<h5><a href="#style" aria-hidden="true" class="anchor" id="style"></a>Style</h5>
<p>The built-in theme uses <a href="https://picocss.com">picocss</a> a very small and simple to use CSS library, Pico makes use of CSS variables so it is very straightforward to customize the theme, placing a <code>custom.css</code> in your content folder and dealing with PicoCSS variables.</p>
<p>Example:</p>
<p><code>mycontent/custom.css</code></p>
<pre><code class="language-css">:root {
  --pico-color: blue;  /* change text color */
  --pico-background-color: #FFCC00; /* change background color */
  --pico-card-background-color: #ccc; /* change background color of articles */
  --pico-container-max-width: 100ch;  /* line Lenght Increase to 100 chars */
  --pico-font-family: &quot;Helvetica&quot;, sans-serif;  /* Different font */
  --pico-border-radius: 4px; /* cards border radius */
  --pico-typography-spacing-vertical: 1rem; /* line spacing */
}
</code></pre>
<p>The CSS looks like a config file, easy to use, and you can also be creative and add more CSS styling than just variables.</p>
<h5><a href="#scripting" aria-hidden="true" class="anchor" id="scripting"></a>Scripting</h5>
<p><strong>Marmite</strong> is very lightweight on JavaScript, it can also be configured to be
javascript-less.</p>
<p>The built-in theme uses Javascript only to:</p>
<ul>
<li>Toggle dark/light modes</li>
</ul>
<p>And on content, optionally enabled on content metadata.</p>
<ul>
<li>Render mermaid diagrams</li>
<li>Render Math syntax</li>
</ul>
<p>Optional features</p>
<ul>
<li>Search
<ul>
<li>When enabled, it will use fuse.js</li>
</ul>
</li>
<li>Comments
<ul>
<li>When enabled it will use a JS script to load comment box</li>
</ul>
</li>
</ul>
<hr />
<h4><a href="#information-architecture" aria-hidden="true" class="anchor" id="information-architecture"></a>Information Architecture</h4>
<p>The built-in theme is heavily optimized for blogging, so the main content of the
articles are the most important part, besides that some other decisions were made with the blog reader in mind.</p>
<ul>
<li>Fully responsive</li>
<li>Listing pages highlights content title, description, date and tags</li>
<li>Estimated reading time is calculated based on average wpm</li>
<li>No distractions, No side panels, only listing and main content</li>
<li>Content groups for tags, dates and author</li>
<li>Author profile page</li>
<li>Global references can be defined on <code>_references.md</code> and linked on any content.</li>
<li>A custom header and footer can be injected to all pages for global  callouts and alerts.</li>
</ul>
<hr />
<h4><a href="#colorschemes" aria-hidden="true" class="anchor" id="colorschemes"></a>Colorschemes</h4>
<p>Marmite comes with some colorschemes built-in, colorschemes are CSS style files that customizes colors, spacing etc.</p>
<p>if you use other applications such
as text editors, terminals and web apps you probably are familiarized with the colorschemes available.</p>
<p>To choose a colorscheme add to config</p>
<p><code>marmite.yaml</code></p>
<pre><code class="language-yaml">extra:
  colorscheme: gruvbox
</code></pre>
<p>The built-in options are <strong>catppuccin</strong>, <strong>clean</strong>, <strong>dracula</strong>, <strong>github</strong>, <strong>gruvbox</strong>, <strong>iceberg</strong>, <strong>monokai</strong>, <strong>nord</strong>, <strong>one</strong>, <strong>solarized</strong>, <strong>typewriter</strong>.</p>
<p>Click to select a colorscheme: <select name="colorscheme" class="colorscheme-toggle"><option value="default">default</option></select><span class="theme-toggle secondary" title="dark mode">☽</span></p>
<p>To create a custom colorscheme drop a <code>custom.css</code> on your input folder (the same where marmite.yaml is located)</p>
<details>
<summary> CLICK HERE to see an example colorscheme on <code>custom.css</code> </summary>
<p><code>custom.css</code></p>
<pre><code class="language-css">/* Marmite Nord Theme */
/* picocss.com  */
:root {
    --pico-border-radius: 0;
}

.content-tags a:where(a:not([role=button])),
[role=link] {
    --pico-color: revert;
}

[data-theme=light],
:root:not([data-theme=dark]) {
    --pico-background-color: #ECEFF4;
    --pico-card-background-color: #E5E9F0;
    --pico-card-sectioning-background-color: var(--pico-background-color);
    --pico-primary: #5E81AC;
    --pico-primary-hover: #81A1C1;
    --pico-color: #2E3440;
    --pico-tag: #4C566A;
    --pico-h1-color: var(--pico-primary);
    --pico-code-background-color: var(--pico-background-color);
    --pico-table-border-color: var(--pico-card-background-color);
    --pico-color-azure-550: var(--pico-primary);
}

[data-theme=light] pre:has(&gt; code.language-mermaid) {
    background-color: var(--pico-card-background-color);
}

[data-theme=dark],
:root:not([data-theme=light]) {
    --pico-background-color: #2E3440;
    --pico-card-background-color: #3B4252;
    --pico-card-sectioning-background-color: var(--pico-background-color);
    --pico-primary: #81A1C1;
    --pico-primary-hover: #88C0D0;
    --pico-color: #D8DEE9;
    --pico-tag: #4C566A;
    --pico-h1-color: var(--pico-color);
    --pico-code-background-color: var(--pico-background-color);
    --pico-table-border-color: var(--pico-card-background-color);
    --pico-color-azure-550: var(--pico-primary);
}

[data-theme=dark] pre:has(&gt; code.language-mermaid) {
    background-color: var(--pico-code-color);
}
</code></pre>
<blockquote>
<p>[!NOTE]<br />
Multiple colorschemes can also be added to <code>static/colorschemes/{name}.css</code>
and then enable <code>extra.colorscheme_toggle</code> on config.</p>
</blockquote>
</details>
<hr>
<h4><a href="#custom-themes" aria-hidden="true" class="anchor" id="custom-themes"></a>Custom Themes</h4>
<p>A theme on marmite is based on <code>static</code> and <code>templates</code> folder, you
can just start from scratch and create your own theme (please share with us if you do).</p>
<p>Marmite CLI also comes with a <code>--start-theme</code> command that will take the built-in theme and copy to your folder, so you can fully customize the templates and static files, read more on <a href="./customizing-templates.html">customizing templates</a>.</p>
<hr />
<h2><a href="#content-types" aria-hidden="true" class="anchor" id="content-types"></a>Content Types</h2>
<p><strong>Marmite</strong> does not complicate in terms of content types and taxonomies, there are only 2 types of content <code>Post</code> and <code>Page</code>, and only 2 types of taxonomy <code>Tags</code> and <code>Stream</code>.</p>
<p>You can read more in details on <a href="content-types.html" data-wikilink="true">content-types</a></p>
<hr />
<h2><a href="#generated-site" aria-hidden="true" class="anchor" id="generated-site"></a>Generated Site</h2>
<p>The site generated by <strong>Marmite</strong> is a flat HTML site, it means that even if the input folder has content organized in subfolder for better writting experience,
the <strong>final</strong> website will always be <strong>flat</strong>.</p>
<p>What is a flat site?</p>
<ul>
<li>There are no subfolders, every page is served from <code>/{slug}.ext</code></li>
<li>There are extensions, content will have <code>.html</code> and feeds will have <code>.rss|.json</code></li>
</ul>
<p>Why this was decided?</p>
<p>Flat sites makes it easier to:</p>
<ul>
<li>Cross link pages and media</li>
<li>Open the site directly in the browser without the need for a server</li>
<li>Customize the templates</li>
<li>Extend functionalities</li>
</ul>
<p>This is a <strong>opinion</strong> that some people don't like, because it is common to use what
people call <del>pretty URLs</del>, e.g: <code>/page</code> instead of <code>/page.html</code> however Marmite opinion
is that <code>.html</code> does't make the URL ugly, HTML is an amazing technology and there is
no problem on showing that our content is published on a static HTML site.</p>
<p>Semantically it is better to have the extension because it is easier to distinguish a folder or an API endpoint from the final content.</p>
<p>If for some reason you don't like the URLs you can still customize it via your webserver, you can configure NGINX for example to do a transparent rewrite of URLS to serve <code>.html</code> content, but is directly supported by Marmite.</p>
<blockquote>
<p>Please consider giving a ☆ on Marmite <a href="https://github.com/rochacbruno/marmite">Github</a> repository, that helps a lot!</p>
</blockquote>
<!-- Contents from _markdown_footer.md are appended to every content -->
<!-- _references.md is a file to gather all references together 
this file is appended to every raw markdown.
-->
]]></content:encoded></item><item><title>Content Types and Taxonomy</title><link>https://www.myblog.com/blog//content-types.html</link><author>rochacbruno</author><guid>https://www.myblog.com/blog//content-types.html</guid><pubDate>Sun, 20 Oct 2024 00:00:00 GMT</pubDate><source url="https://www.myblog.com/blog/">index</source><content:encoded><![CDATA[<p>Marmite separates content in two kinds, <strong>posts</strong> and <strong>pages</strong>.</p>
<p>An <strong>opinionated</strong> decision of marmite is how it makes the distinction.</p>
<h3><a href="#posts" aria-hidden="true" class="anchor" id="posts"></a>Posts</h3>
<p>If content has a <strong>date</strong> it is a <strong>Post</strong>!</p>
<p>Instead of having to mark if a content is a page via metadata, <strong>Marmite</strong> takes the
simplicity to just decided based on if the content is chronological or static.</p>
<h4><a href="#date-discovery" aria-hidden="true" class="anchor" id="date-discovery"></a>Date discovery</h4>
<p>There are 2 ways to tell <strong>Marmite</strong> that your content is a <strong>Post</strong>:</p>
<ul>
<li>
<p>Option 1: <code>date:</code> on <strong>frontmatter</strong> in any of the valid formats</p>
<pre><code class="language-yaml">---
date: 2024-10-20
---
# Hello
</code></pre>
<p>Valid formats:</p>
<pre><code class="language-bash">2024-01-01
2024-01-01 15:40
2024-01-01-15:40
2024-01-01 15:40:56
2024-01-01-15:40:56
2024-01-01T15:40
2024-01-01T15:40:56
</code></pre>
<p>Optional precision and timezone suffixes are ignored:
<code>.123Z, +0000, .123+0000, .123+0000Z</code></p>
</li>
<li>
<p>Option 2: <strong>filename</strong> with a valid date prefix</p>
<pre><code class="language-console">$ ls mycontent
my-file.md
2024-01-01-my-file.md
2024-01-01-15-30-my-file.md
2024-01-01-15-30-12-my-file.md
2024-01-01T15:30-my-file.md
2024-01-01T15:30:12-my-file.md
</code></pre>
</li>
</ul>
<h4><a href="#where-posts-are-listed" aria-hidden="true" class="anchor" id="where-posts-are-listed"></a>Where posts are listed?</h4>
<ul>
<li>By default on <code>/index.html</code> ordered by <strong>date</strong> (newest first)
<ul>
<li>If the content defines a custom stream, then the post is not listed on index,
and appears only on the custom stream.
<code>content.md</code>
<pre><code>---
stream: notes
---
# my first note
</code></pre>
This post will not show on <code>index.html</code> but will on <code>/notes.html</code><br />
This post will not show on <code>index.rss</code> but will on <code>/notes.rss</code></li>
</ul>
</li>
<li>Taxonomies
<ul>
<li><code>/tags.html</code> will list content grouped by tags
<ul>
<li><code>/tag-{name}.html</code> will list content for the <code>name</code> tag.</li>
</ul>
</li>
<li><code>/archive.html</code> will list content grouped by year
<ul>
<li><code>/archive-{year}.html</code> will list content for the <code>year</code></li>
</ul>
</li>
<li><code>/authors.html</code> will list content grouped by author
<ul>
<li><code>/author-{username}.html</code> will list content by author <code>username</code></li>
</ul>
</li>
</ul>
</li>
</ul>
<h4><a href="#formatting" aria-hidden="true" class="anchor" id="formatting"></a>Formatting</h4>
<p><strong>Marmite</strong> parses the markdown content using <strong>CommonMark</strong> and <strong>Github Flavoured Markdown</strong>, it allows raw HTML and adds some extensions to enable Wikilinks, Obsidian Links, Backlinks, Spoilers, Footnotes and the built-in theme supports <strong>mermaid</strong> and <strong>math</strong> syntax.</p>
<p>You can see examples on <a href="markdown-format.html" data-wikilink="true">markdown-format</a></p>
<h4><a href="#metadata" aria-hidden="true" class="anchor" id="metadata"></a>Metadata</h4>
<p>On each markdown file it is possible (and optional) to define metadata on the <strong>FrontMatter</strong></p>
<p>Marmite supports 3 formats for the front matter.</p>
<p><strong>YAML</strong> separated by <code>---</code>. (<strong>recommended</strong>)</p>
<pre><code class="language-yaml">---
field: value
---
</code></pre>
<p><strong>TOML</strong> separated by <code>+++</code>.</p>
<pre><code class="language-yaml">+++
field = &quot;value&quot;
+++
</code></pre>
<p><strong>JSON</strong> enclosed in <code>{ }</code>. (<strong>NOT recommended</strong>, but supported for compatibility)</p>
<pre><code class="language-yaml">{
  &quot;field&quot;: &quot;value&quot;
}
</code></pre>
<p>Example:</p>
<pre><code class="language-yaml">---
field: value
---

# title

Content
</code></pre>
<p><code>marmite</code> supports the following fields:</p>
<dl>
<dt><strong>title</strong></dt>
<dd>
<p>str: Title of the post<br />
<strong>default</strong>: extracted from the first line of markdown.</p>
</dd>
<dt><strong>description</strong></dt>
<dd>
<p>str: Description text (for listing and RSS)<br />
<strong>default</strong>: extracted from the content first sentence.</p>
</dd>
<dt><strong>slug</strong></dt>
<dd>
<p>str: this-is-the-post-slug<br />
<strong>default</strong>: slugfied <code>title</code> or <code>filename</code>.</p>
</dd>
<dt><strong>date</strong></dt>
<dd>
<p>str: <code>YYYY-MM-DD</code><br />
<strong>formats</strong> <code>YYYY-MM-DD</code>, <code>YYYY-MM-DD HH:MM</code>, <code>YYYY-MM-DD HH:MM:SS</code><br />
<strong>default</strong>: extracted from filename or null.</p>
</dd>
<dt><strong>tags</strong></dt>
<dd>
<p>Comma separated list of tags, or YAML list of tags<br />
<strong>formats</strong></p>
<pre><code class="language-yaml">tags: tag1, tag2, tag3
tags:
  - tag1
  - tag2
</code></pre>
<p><strong>default</strong> empty</p>
</dd>
<dt><strong>authors</strong></dt>
<dd>
<p>Single author, comma separated list of authors, or YAML list of authors<br />
<strong>formats</strong></p>
<pre><code class="language-yaml">author: username1
authors: username1, username2, username3
authors:
  - username1
  - username2
</code></pre>
<p><strong>default</strong> empty<br />
<strong>important</strong> authors are identified by the username, and if there is a matching author in the config file then the data is used to build the profile page on <code>author-{username}.html</code> with avatar, bio and links.</p>
</dd>
<dt><strong>stream</strong></dt>
<dd>
<p>str: something<br />
<strong>default</strong>: index<br />
<strong>important</strong>: Stream is used to define a separate index for the content, <code>something.html</code> will be the list of contents for a stream.</p>
</dd>
<dt><strong>card_image</strong></dt>
<dd>
<p>Image url to use as social card image <code>og:image</code>, this is the image that shows when you share your link on mastodon, bluesky, linkedin etc.<br />
<strong>format</strong>  <code>media/file.png</code> or <code>https://path/to/img.jpg</code><br />
<strong>default</strong> banner_image or first image extracted from html content or config card_image, or None.</p>
</dd>
<dt><strong>banner_image</strong></dt>
<dd>
<p>Image url to use the top banner on content page<br />
<strong>format</strong>  <code>media/file.png</code> or <code>https://path/to/img.jpg</code><br />
<strong>default</strong> empty</p>
</dd>
<dt><strong>extra</strong></dt>
<dd>
<p>arbitrary extra <code>key:value</code> pair in YAML format (for template customization)<br />
<strong>format</strong></p>
<pre><code class="language-yaml">extra:
  math: true
  mermaid: true
</code></pre>
<p><strong>important</strong>: the above example shows the keys supported by the default theme.</p>
</dd>
<dt><strong>pinned</strong></dt>
<dd>
<p>Boolean <code>true</code> or <code>false</code> indicating if content is pinned on top
of its stream page.
<strong>default</strong> false</p>
</dd>
<dt><strong>toc</strong></dt>
<dd>
<p>Boolean <code>true</code> or <code>false</code> indicating if Table of contents must be generated and included to the content.
<strong>default</strong> false</p>
</dd>
</dl>
<h3><a href="#pages" aria-hidden="true" class="anchor" id="pages"></a>Pages</h3>
<p>If the markdown file doesn't define a date, then <strong>Marmite</strong> can't list it on index or feeds, because it doesn't know where to include it in the chronological order, so it makes sense to render this content as a page named <code>{slug}.html</code> and make it accessible only via the link directly, so it can optionally added to the main menu or linked in other content.</p>
<h3><a href="#menu" aria-hidden="true" class="anchor" id="menu"></a>Menu</h3>
<p>By default marmite includes items in the main menu:</p>
<p><strong>Tags</strong> -&gt; <code>tags.html</code></p>
<p>:Content grouped by tags</p>
<p><strong>Archive</strong> -&gt; <code>archive.html</code></p>
<p>:Content grouped by year</p>
<p><strong>Authors</strong> -&gt; <code>authors.html</code></p>
<p>:Content grouped by author</p>
<p><strong>Streams</strong> -&gt; <code>streams.html</code></p>
<p>:Content grouped by stream</p>
<dl>
<dt><strong>Pages</strong> -&gt; <code>pages.html</code></dt>
<dd>
<p>List of pages in alphabetical order.</p>
</dd>
</dl>
<h4><a href="#customizing-the-menu" aria-hidden="true" class="anchor" id="customizing-the-menu"></a>Customizing the menu</h4>
<p>The menu is defined on <code>marmite.yaml</code> config file and is a list of tuples
containing name and URL</p>
<p><code>marmite.yaml</code></p>
<pre><code class="language-yaml">menu:
  - - About
    - about.html
  - - Github
    - https://github.com/me
</code></pre>
<p>You can add anything you want to the menu and the name allows HTML.</p>
<blockquote>
<p>When <code>search_enabled: true</code> is on your config marmite will also add an icon to open the search spotlight on the menu, also the light/dark mode switch.</p>
</blockquote>
<h3><a href="#taxonomies-and-groups" aria-hidden="true" class="anchor" id="taxonomies-and-groups"></a>Taxonomies and groups</h3>
<p>Taxonomy is a way to group or separate content based on marks, <strong>Marmite</strong> allows you to add the following marks to the content.</p>
<h4><a href="#group-content-together" aria-hidden="true" class="anchor" id="group-content-together"></a>Group content together</h4>
<h5><a href="#authors" aria-hidden="true" class="anchor" id="authors"></a>Authors</h5>
<p>Add <code>author: username</code> or <code>authors: username, other</code> to the content.</p>
<p>A page named <code>authors.html</code> <a href="authors.html" data-wikilink="true">authors</a> is rendered and can be included in the menu,
it groups all content for authors.</p>
<h5><a href="#tags" aria-hidden="true" class="anchor" id="tags"></a>Tags</h5>
<p>Add <code>tags: one,two</code> to the content.</p>
<p>A page named <code>tags.html</code> <a href="tags.html" data-wikilink="true">tags</a> is rendered and can be included in the menu, or linked in any other page, this goups content by tags.</p>
<h5><a href="#archive" aria-hidden="true" class="anchor" id="archive"></a>Archive</h5>
<p>Add <code>data: YYYY-MM-DD</code> to the content and a page named <code>archive.html</code> <a href="archive.html" data-wikilink="true">archive</a> is rendered grouping content by year.</p>
<p>Each of the pages above will also have its equivalent <code>.rss</code> feed.</p>
<h4><a href="#separating-content" aria-hidden="true" class="anchor" id="separating-content"></a>Separating content</h4>
<h5><a href="#streams" aria-hidden="true" class="anchor" id="streams"></a>Streams</h5>
<p>Add <code>stream: name</code> to the content.</p>
<p>Streams are a way to have separate index on the site, you can check all available
streams on <code>streams.html</code> page <a href="streams.html" data-wikilink="true">streams</a>.</p>
<p>Each stream have its separate RSS feed and listing page.</p>
<hr />
<h3><a href="#media" aria-hidden="true" class="anchor" id="media"></a>Media</h3>
<p>Images can be added using the normal markdown tag, marmite doesn't have shortcodes yet.</p>
<p>For local images you have to put the files in a folder named <code>media</code> in the content folder.</p>
<pre><code class="language-markdown"># content with media

![Image here](media/subfolder/image.png)
</code></pre>
<p>Marmite will copy your <code>media</code> folder to the output site/</p>
<h3><a href="#site-config" aria-hidden="true" class="anchor" id="site-config"></a>Site Config</h3>
<p>Optionally, a file named <code>marmite.yaml</code> inside your content folder (together with your .md files)
can be used to customize configuration.</p>
<blockquote>
<p><code>--config file.yaml</code> can also be passed directly to the CLI.</p>
</blockquote>
<p>example:</p>
<pre><code class="language-yaml">name: My Blog
tagline: Poems, Essays and Articles
url: https://mysite.com/blog
menu:
  - [&quot;About&quot;, &quot;about.html&quot;]
  - [&quot;Projects&quot;, &quot;projects.html&quot;]
  - [&quot;Contact&quot;, &quot;contact.html&quot;]
  - [&quot;Github&quot;, &quot;https://github.com/rochacbruno&quot;]
extra:
  colorscheme: gruvbox
  fediverse_verification: https://mastodon.social/@me
</code></pre>
<p>Other options are available and can be viewed on <a href="https://github.com/rochacbruno/marmite/blob/main/example/marmite.yaml">repository</a></p>
<p>Use <code>--generate-config</code> in the CLI to create a default config file.</p>
<h2><a href="#theme-customization" aria-hidden="true" class="anchor" id="theme-customization"></a>Theme customization</h2>
<p>The embedded templates are created with <a href="https://picocss.com/">picocss.com</a> and
it is easy to customize, just put a <code>custom.css</code> and a <code>custom.js</code>
in your root folder and use anything that pico supports or just be creative with css.</p>
<p>If customizing the css and js is not enough then you can create your own theme.</p>
<h2><a href="#creating-a-new-theme" aria-hidden="true" class="anchor" id="creating-a-new-theme"></a>Creating a new Theme</h2>
<p>To create a new theme is very simple, you just need to add to your content folder
the <code>templates</code> and <code>static</code> directories and then customize in the way you like.</p>
<p>Use <code>--start-theme</code> on the CLI to start a new theme from the built-in theme.</p>
<p>To learn more about how to create a new theme check this post:</p>
<p><a href="customizing-templates.html">Customizing Templates</a></p>
<hr />
<p><a href="tag-docs.html">Read the Docs</a></p>
<blockquote>
<p>Please consider giving a ☆ on Marmite <a href="https://github.com/rochacbruno/marmite">Github</a> repository, that helps a lot!</p>
</blockquote>
<!-- Contents from _markdown_footer.md are appended to every content -->
<!-- _references.md is a file to gather all references together 
this file is appended to every raw markdown.
-->
]]></content:encoded></item><item><title>Customizing Templates</title><link>https://www.myblog.com/blog//customizing-templates.html</link><author>rochacbruno</author><category>docs</category><category>templates</category><category>theme</category><category>customization</category><guid>https://www.myblog.com/blog//customizing-templates.html</guid><pubDate>Fri, 18 Oct 2024 11:00:00 GMT</pubDate><source url="https://www.myblog.com/blog/">index</source><content:encoded><![CDATA[<p>Marmite uses <a href="https://keats.github.io/tera/docs/#templates">Tera</a> as its template
parser, the language is very similar to <strong>Jinja</strong> or <strong>Twig</strong>.</p>
<blockquote>
<p>[!IMPORTANT]<br />
always link relative to the current path starting with <code>./</code><br />
If absolute url is needed then use <code>{{ url_for(path=&quot;path&quot;, abs=true) }}</code> template function.</p>
</blockquote>
<p>Example on <code>templates/list.html</code></p>
<pre><code class="language-html">{% extends &quot;base.html&quot; %}
{% block main %}
  &lt;div class=&quot;content-list&quot;&gt;
  {%- for content in content_list %}
    &lt;h2 class=&quot;content-title&quot;&gt;&lt;a href=&quot;./{{content.slug}}.html&quot;&gt;{{ content.title | capitalize }}&lt;/a&gt;&lt;/h2&gt;
    &lt;p class=&quot;content-excerpt&quot;&gt;{{ content.html | striptags | truncate(length=100, end=&quot;...&quot;) }}&lt;/p&gt;
  {%- endfor %}
  &lt;/div&gt;
{% endblock %}
</code></pre>
<h2><a href="#starting-a-new-theme" aria-hidden="true" class="anchor" id="starting-a-new-theme"></a>Starting a new theme</h2>
<pre><code class="language-console">$ marmite input_folder output_folder --start-theme
Generated input_folder/templates
Generated input_folder/static
</code></pre>
<p>Then you can start customizing those assets.</p>
<p>If instead, you just want to customize the templates use <code>--init-templates</code>
and the <code>custom.css</code> and <code>custom.js</code> in the root of the project.</p>
<h2><a href="#templates" aria-hidden="true" class="anchor" id="templates"></a>Templates</h2>
<p>all templates are rendered with the global context.</p>
<pre><code class="language-yaml">site_data:
  posts: [Content]
  pages: [Content]
  tag: GroupedContent
  archive: GroupedContent
  author: GroupedContent
  stream: GroupedContent
site:
  name: str
  url: str
  tagline: str
  pagination: int
  extra: {k, v}
  ...the keys on site configuration.
menu: [[name, link]]
</code></pre>
<p>The <code>Content</code> object can be a <strong>page</strong> or a <strong>post</strong> and contains</p>
<pre><code class="language-yaml">title: str
slug: str
html: str
tags: [str] or []
date: DateTimeObject or None
extra: {key: value}
links_to: [str] or None
back_links: [Content] or []
card_image: str or None
banner_image: str or None
authors: [str] or []
pinned: bool
toc: str or None
</code></pre>
<p>The <code>GroupedContent</code> is a map that when iterated returns a map of <code>name: [Content]</code>, this is available on global context,
which means <code>tag</code>, <code>archive</code> and <code>author</code> groups can be accessed on any template, however it is not recommended to access it directly,
because those are unordered, to get an ordered version use <code>group</code> function:</p>
<pre><code class="language-html">&lt;ul class=&quot;content-tags&quot;&gt;
    {% for name, items in group(kind=&quot;tag&quot;) -%}  &lt;!-- kind can be one of [tag,archive,author] --&gt;
        &lt;li&gt;&lt;a href=&quot;./tag-{{ name | trim | slugify }}.html&quot;&gt;{{ name }}&lt;/a&gt;&lt;span class=&quot;tag-count&quot;&gt; [{{ items | length }}]&lt;/span&gt;&lt;/li&gt;
    {%- endfor %}
&lt;/ul&gt;
</code></pre>
<p>There are 10 templates inside the <code>templates</code> folder, each adds more data to context.</p>
<ul>
<li>base.html
<ul>
<li>All other templates inherits blocks from this one.</li>
</ul>
</li>
<li>list.html
<ul>
<li>Renders <code>index.html</code>, <code>pages.html</code>, <code>tag-{name}.html</code>, <code>archive-{year}.html</code>, <code>author-{username}.html</code></li>
<li>adds <code>title:str</code>, <code>content_list: [Content]</code>,</li>
<li>adds <code>author: Author</code> when rendering <code>author-*.html</code> pages.</li>
<li>pagination: <code>current_page: str</code>, <code>next_page:str</code>, <code>previous_page:str</code>,
<code>total_pages:int</code>, <code>current_page_number:int</code>, <code>total_content:int</code></li>
</ul>
</li>
<li>content.html
<ul>
<li>Renders individual content page <code>my-post.html</code></li>
<li>adds <code>title:str</code>, <code>content: [Content]</code>, <code>current_page: str</code></li>
</ul>
</li>
<li>group.html
<ul>
<li>Renders grouped information such as <code>tags.html</code> and <code>archive.html</code></li>
<li>adds <code>title:str</code>, <code>current_page: str</code>, <code>kind:str</code></li>
</ul>
</li>
</ul>
<p>Include templates:</p>
<p>Those are templates that are not meant to be rendered directly, those are included on other templates.</p>
<ul>
<li>pagination.html
<ul>
<li>Render the pagination controls</li>
<li>included on <code>list.html</code></li>
</ul>
</li>
<li>comments.html
<ul>
<li>Render the comment box</li>
<li>included on <code>content.html</code></li>
</ul>
</li>
<li>content_authors.html
<ul>
<li>Renders the small list of authors on botton of content</li>
<li>included on <code>content.html</code></li>
</ul>
</li>
<li>content_date.html
<ul>
<li>Renders the article date</li>
<li>included on <code>content.html</code> and <code>content_title.html</code></li>
</ul>
</li>
<li>content_title.html
<ul>
<li>Renders the main title of the content + date and reading time</li>
<li>included on <code>content.html</code></li>
</ul>
</li>
<li>group_author_avatar.html
<ul>
<li>Renders the author avatar image</li>
<li>included on <code>group.html</code> (if author is defined)</li>
</ul>
</li>
</ul>
<p>When customizing the templates you can create new templates to use as <code>include</code> or <code>macro</code> but the 4 listed above are required.</p>
<p>If you just want to customize some individual template you can add only it in the
templates/ folder and the rest will be added by marmite.</p>
<p>See the templates on: <a href="https://github.com/rochacbruno/marmite/tree/main/example/templates">https://github.com/rochacbruno/marmite/tree/main/example/templates</a></p>
<h2><a href="#customizing-the-index-template" aria-hidden="true" class="anchor" id="customizing-the-index-template"></a>Customizing the index template</h2>
<p>In the case you want to keep using the built-in theme but want to customize the index page of your site you can
just add a <code>templates/custom_index.html</code> to your input folder.</p>
<p>This template will be used for the index folder instead of the <code>list.html</code> and you can do whatever you want on it,
using the objets and functions available on the context.</p>
<p>Example of a custom index template.</p>
<details>
<summary> <strong>Click Here</strong> to see the code for an example `templates/custom_index.html` </summary>
<pre><code class="language-html">{% extends &quot;base.html&quot; %}
{% block head %}
    {# this head block is not needed if CSS is moved to custom.css #}
    {{ super() }}
    &lt;style&gt;
    .content-banner-image {
        background-size: cover;
        background-position: center;
        height: 300px;
        margin-bottom: 20px;
        background-image: url('media/og_image.jpg');
    }
    .main-div {
        display: flex;
        grid-column-gap: 10px;
        grid-row-gap: 0px;
    }
    .left {
        width: 70%;
    }
    .right {
        width: 30%;
    }
    .right li {
        list-style-type: none;
    }
    .author-card img {
        width: 150px;
        height: 150px;
        border-radius: 50%;
    }
    @media (max-width: 1024px) {
        .main-div {
            display: block;
        }
        .left {
            width: 100%;
        }
        .right {
            width: 100%;
        }
    }
    &lt;/style&gt;
{% endblock %}

{% block main %}
&lt;div class=&quot;content-banner-image&quot;&gt;&lt;/div&gt;
&lt;div class=&quot;main-div&quot;&gt;
    &lt;div class=&quot;left&quot;&gt;
            {% if hero is defined %}
            &lt;div class=&quot;hero&quot;&gt;
                &lt;article&gt;
                    {{hero}}
                &lt;/article&gt;
            &lt;/div&gt;
            {% endif %}

            {%- for content in content_list %}
            &lt;article class=&quot;content-card&quot;&gt;
                &lt;h2 class=&quot;content-title&quot;&gt;&lt;a href=&quot;./{{content.slug}}.html&quot;&gt;{{ content.title | capitalize }}&lt;/a&gt;&lt;/h2&gt;
                &lt;p class=&quot;content-excerpt&quot;&gt;
                    {% if content.description %}
                    {{ content.description | replace(from='&quot;', to=&quot;&quot;) | truncate(length=250, end=&quot; ...&quot;) }}
                    {% else %}
                    {{ content.html | striptags | trim_start_matches(pat=content.title) | truncate(length=250, end=&quot; ...&quot;) }}
                    {%- endif %}
                    &lt;a class=&quot;secondary&quot; href=&quot;./{{content.slug}}.html&quot;&gt;read more &amp;rarr;&lt;/a&gt;
                &lt;/p&gt;
                {% if content.date -%}
                &lt;footer class=&quot;data-tags-footer&quot;&gt;
                    &lt;span class=&quot;content-date&quot;&gt;&lt;a class=&quot;secondary&quot; href=&quot;./{{content.slug}}.html&quot;&gt;{{ content.date | default_date_format }}&lt;/a&gt;&lt;/span&gt;
                    {% if content.tags -%}
                    &lt;ul class=&quot;content-tags overflow-auto&quot;&gt;
                        {% for tag in content.tags | slice(end=3) -%}
                        &lt;li&gt;&lt;a href=&quot;./tag-{{ tag | trim | slugify }}.html&quot;&gt;{{ tag }}&lt;/a&gt;&lt;/li&gt;
                        {%- endfor %}
                    &lt;/ul&gt;
                    {%- endif %}
                &lt;/footer&gt;
                {%- endif %}
            &lt;/article&gt;
            {%- endfor %}
            {% if total_pages is defined and total_pages &gt; 1 %}
                {% include &quot;pagination.html&quot; ignore missing %}
            {% endif %}
        
    &lt;/div&gt;
    &lt;div class=&quot;right&quot;&gt;
        {% if site.authors %}
            {% for username, author in site.authors %}
            &lt;article class=&quot;author-card&quot;&gt;
                &lt;header&gt;{{author.name}}&lt;/header&gt;
                &lt;img src=&quot;{{author.avatar}}&quot; alt=&quot;{{author.name}}&quot;&gt;
                &lt;p&gt;{{author.bio}}&lt;/p&gt;
                &lt;ul&gt;
                    {% for link in author.links %}
                    &lt;li&gt;&lt;a href=&quot;{{link.1}}&quot;&gt;{{link.0}}&lt;/a&gt;&lt;/li&gt;
                    {% endfor %}
                &lt;/ul&gt;
            &lt;/article&gt;
            {% break %} {# only show the first author #}
            {% endfor %}
        {% endif %}
        &lt;article&gt;
            &lt;header&gt;Tags&lt;/header&gt;
            &lt;ul&gt;
            {% for name, _ in group(kind=&quot;tag&quot;) %}
            &lt;li&gt;&lt;a href=&quot;tag-{{name | slugify}}.html&quot;&gt;#{{name | slugify}}&lt;/a&gt;&lt;/li&gt;
            {% endfor %}
            &lt;/ul&gt;
        &lt;/article&gt;
        &lt;article&gt;
            &lt;header&gt;Authors&lt;/header&gt;
            &lt;ul&gt;
            {% for name, _ in group(kind=&quot;author&quot;) %}
            &lt;li&gt;&lt;a href=&quot;author-{{name | slugify}}.html&quot;&gt;@{{name}}&lt;/a&gt;&lt;/li&gt;
            {% endfor %}
            &lt;/ul&gt;
        &lt;/article&gt;
        &lt;article&gt;
            &lt;header&gt;Archive&lt;/header&gt;
            &lt;ul&gt;
            {% for year, _ in group(kind=&quot;archive&quot;) %}
            &lt;li&gt;&lt;a href=&quot;archive-{{year}}.html&quot;&gt;{{year}}&lt;/a&gt;&lt;/li&gt;
            {% endfor %}
            &lt;/ul&gt;
        &lt;/article&gt;
        &lt;article&gt;
            &lt;header&gt;Streams&lt;/header&gt;
            &lt;ul&gt;
            {% for stream, _ in group(kind=&quot;stream&quot;) %}
            &lt;li&gt;&lt;a href=&quot;{{stream}}.html&quot;&gt;{{stream}}&lt;/a&gt;&lt;/li&gt;
            {% endfor %}
            &lt;/ul&gt;
        &lt;/article&gt;
        &lt;article&gt;
            &lt;header&gt;Pages&lt;/header&gt;
            &lt;ul&gt;
            {% for page in site_data.pages %}
            &lt;li&gt;&lt;a href=&quot;{{page.slug}}.html&quot;&gt;{{page.title}}&lt;/a&gt;&lt;/li&gt;
            {% endfor %}
            &lt;/ul&gt;
        &lt;/article&gt;
    &lt;/div&gt;
&lt;/div&gt; 
{% endblock %}
</code></pre>
</details>
<hr />
<p>With the above HTML your index page will look like:</p>
<figure>
  <figcaption>Custom Index</figcaption>
  <img src="./media/screenshots/custom_index.png" width="600">
</figure>
<blockquote>
<p>[!NOTE]<br />
A custom index can be created for any <strong>stream</strong>, name the template <code>custom_{stream}.html</code></p>
</blockquote>
<h2><a href="#static-files" aria-hidden="true" class="anchor" id="static-files"></a>Static files</h2>
<p>Just create a <code>static</code> folder side by side with your <code>templates</code> folder and add
all <code>css</code>, <code>js</code>, <code>fonts</code> etc on this folder.</p>
<p>Marmite will copy this folder to the output site, if this folder is not found
marmite will then copy the embedded static files to the static folder.</p>
<h2><a href="#url" aria-hidden="true" class="anchor" id="url"></a>URL</h2>
<p>Prefer to use relative paths for URLS, examples:</p>
<ul>
<li><code>./my-blog-post.html</code></li>
<li><code>./static/style.css</code></li>
<li><code>./media/photo.png</code></li>
</ul>
<p>This is recommended because <strong>marmite</strong> will always generate a <strong>flat</strong> html website,
there is no subpaths.</p>
<p>If you need absolute url use the <code>url_for</code> function to refer to urls.</p>
<pre><code class="language-html">{{ url_for(path='static/mystyle.css', abs=true) }}
</code></pre>
<h2><a href="#extra-data" aria-hidden="true" class="anchor" id="extra-data"></a>Extra data</h2>
<p>On site config <code>marmite.yaml</code> there is an arbitrary field <code>extra</code> that can be accessed
on any template.</p>
<pre><code class="language-yaml">extra:
  myname: Bruno
</code></pre>
<p>Then on an template.</p>
<pre><code class="language-html">{{site.extra.myname}}
</code></pre>
<p>On each individual post there is a <code>extra</code> arbitrary field, so on <code>list.html</code> and
<code>content.html</code> this field can also be accessed.</p>
<pre><code class="language-markdown">---
extra:
  image: media/banner.jpg
---
</code></pre>
<p>then on template</p>
<pre><code class="language-html">&lt;img src=&quot;{{url_for(content.extra.image)}}&quot;&gt;
</code></pre>
<h2><a href="#raw-html-on-markdown" aria-hidden="true" class="anchor" id="raw-html-on-markdown"></a>Raw HTML on markdown</h2>
<p>Tera is configured to allow raw html on markdown, so any html tag will be
allowed, a markdown file can include for example embeds, scripts, etc..</p>
<h2><a href="#tera-object" aria-hidden="true" class="anchor" id="tera-object"></a>Tera Object</h2>
<p>Here the Tera object for reference,
to see each individual filter or tester documentation read <a href="https://keats.github.io/tera/docs/#templates">https://keats.github.io/tera/docs/#templates</a></p>
<pre><code class="language-rust">Tera {
    templates: [
            group.html,
            base.html,
            list.html,
            content.html,
            pagination.html,
            comments.html,
    ]
    functions: [
            group,
            url_for,
    ]
    filters: [
            default_date_format,
            reverse,
            trim_start,
            trim,
            unique,
            float,
            trim_end,
            join,
            group_by,
            lower,
            trim_start_matches,
            wordcount,
            title,
            trim_end_matches,
            indent,
            filter,
            map,
            pluralize,
            upper,
            first,
            slice,
            last,
            round,
            length,
            date,
            filesizeformat,
            urlencode,
            urlencode_strict,
            nth,
            escape_xml,
            truncate,
            striptags,
            abs,
            linebreaksbr,
            spaceless,
            slugify,
            addslashes,
            capitalize,
            escape,
            split,
            int,
            concat,
            as_str,
            sort,
            replace,
            get,
            json_encode,
    ]
    testers: [
            number,
            object,
            ending_with,
            defined,
            odd,
            matching,
            even,
            containing,
            iterable,
            undefined,
            string,
            starting_with,
            divisibleby,
    ]
}
</code></pre>
<blockquote>
<p>Please consider giving a ☆ on Marmite <a href="https://github.com/rochacbruno/marmite">Github</a> repository, that helps a lot!</p>
</blockquote>
<!-- Contents from _markdown_footer.md are appended to every content -->
<!-- _references.md is a file to gather all references together 
this file is appended to every raw markdown.
-->
]]></content:encoded></item><item><title>Using Markdown to customize layout</title><link>https://www.myblog.com/blog//using-markdown-to-customize-layout.html</link><author>rochacbruno</author><category>docs</category><category>templates</category><category>theme</category><category>customization</category><guid>https://www.myblog.com/blog//using-markdown-to-customize-layout.html</guid><pubDate>Fri, 18 Oct 2024 10:00:00 GMT</pubDate><source url="https://www.myblog.com/blog/">index</source><content:encoded><![CDATA[<h2><a href="#listing-page" aria-hidden="true" class="anchor" id="listing-page"></a>Listing Page</h2>
<p>The following fragments allow <strong>markdown</strong> and <strong>HTML</strong> and are rendered by <strong>Tera</strong> so it is possible to use template expressions.</p>
<p>The global context is available for templating.</p>
<ul>
<li><code>_announce.md</code></li>
<li><code>_header.md</code></li>
<li><code>_hero.md</code></li>
<li><code>_sidebar.md</code></li>
<li><code>_footer.md</code></li>
</ul>
<p><figure><img src="media/fragments_index.png" alt="listing" /></figure></p>
<blockquote>
<p>The listing itself is only customizable via <code>templates/list.html</code> or <code>templates/custom_{stream}.html</code></p>
</blockquote>
<h2><a href="#content-page" aria-hidden="true" class="anchor" id="content-page"></a>Content Page</h2>
<p>The following are <strong>static</strong> content <strong>NOT</strong> rendered by template engine so
those allow only raw <strong>HTML</strong> and <strong>markdown</strong></p>
<ul>
<li><code>_markdown_header.md</code></li>
<li><code>_markdown_footer.md</code></li>
</ul>
<p>The <code>_comments.md</code> allows template rendering with global context.</p>
<ul>
<li><code>_comments.md</code></li>
</ul>
<p><figure><img src="media/fragments_content.png" alt="listing" /></figure></p>
<blockquote>
<p>The Title, Main content, Info and Related content are customizable only via
<code>templates/content.html</code></p>
</blockquote>
<h2><a href="#references" aria-hidden="true" class="anchor" id="references"></a>References</h2>
<ul>
<li><code>_references.md</code></li>
</ul>
<p>This is an invisible file that is appended to every other markdown so links
and footnotes are reusable.</p>
<p><code>_references.md</code></p>
<pre><code class="language-markdown">[Github]: https://github.com
</code></pre>
<p>then on any markdown <code>[Github]</code> will resolve to the reference link.</p>
<h2><a href="#scripting-and-style-blocks" aria-hidden="true" class="anchor" id="scripting-and-style-blocks"></a>Scripting and Style blocks</h2>
<p>You can add extra scripts and styles to the <code>&lt;head&gt;</code> tag using <code>_htmlhead.md</code></p>
<p><code>_htmlhead.md</code></p>
<pre><code class="language-html">&lt;script defer src=&quot;https://analytics.service.com&quot; data-website-id=&quot;c8fbd715-5b90&quot;&gt;&lt;/script&gt;
&lt;style&gt;
selector {
    property: value;
}
&lt;/style&gt;
</code></pre>
<p>If the script needs to go to the bottom of page use <code>_htmltail.md</code></p>
<p>That is available mostly to be used as analytics entrypoint, for custom styling we recommend
using the <code>custom.css</code></p>
<blockquote>
<p>Please consider giving a ☆ on Marmite <a href="https://github.com/rochacbruno/marmite">Github</a> repository, that helps a lot!</p>
</blockquote>
<!-- Contents from _markdown_footer.md are appended to every content -->
<!-- _references.md is a file to gather all references together 
this file is appended to every raw markdown.
-->
]]></content:encoded></item><item><title>Markdown Formatting Options</title><link>https://www.myblog.com/blog//markdown-format.html</link><description><![CDATA["The content on Marmite accepts any valid CommonMark or Github Flavoured markdown and some GFM extensions."]]></description><author>rochacbruno</author><category>docs</category><category>markdown</category><category>Common Mark</category><category>GFM</category><guid>https://www.myblog.com/blog//markdown-format.html</guid><pubDate>Thu, 17 Oct 2024 12:00:01 GMT</pubDate><source url="https://www.myblog.com/blog/">index</source><content:encoded><![CDATA[<h2><a href="#common-markdown" aria-hidden="true" class="anchor" id="common-markdown"></a>Common Markdown</h2>
<p>The content on Marmite accepts any valid <code>CommonMark</code> or <strong>Github</strong> <em>Flavoured</em> markdown
and some <code>GFM</code> extensions, Marmite also does post processing of HTML to support features such as
back-links and Obsidian links.</p>
<h3><a href="#paragraphs-and-formatting" aria-hidden="true" class="anchor" id="paragraphs-and-formatting"></a>Paragraphs and formatting</h3>
<p>Simple paragraph and usual formatting like <strong>bold</strong>, <u>underline</u>, <em>italic</em>
and also all sorts of formatting elements, as follows.</p>
<pre><code class="language-markdown">**bold**, __underline__, *italic*
</code></pre>
<h3><a href="#strike-through" aria-hidden="true" class="anchor" id="strike-through"></a>Strike-through</h3>
<p>The following is <del>no more</del>.</p>
<pre><code class="language-markdown">The following is ~~no more~~.
</code></pre>
<h3><a href="#table" aria-hidden="true" class="anchor" id="table"></a>Table</h3>
<table>
<thead>
<tr>
<th>Syntax</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>Header</td>
<td>Title</td>
</tr>
<tr>
<td>List</td>
<td>Here's a list! <ul><li>Item one.</li><li>Item two.</li></ul></td>
</tr>
</tbody>
</table>
<pre><code class="language-markdown">| Syntax      | Description |
| ----------- | ----------- |
| Header      | Title |
| List        | Here's a list! &lt;ul&gt;&lt;li&gt;Item one.&lt;/li&gt;&lt;li&gt;Item two.&lt;/li&gt;&lt;/ul&gt; |
</code></pre>
<h3><a href="#footnotes" aria-hidden="true" class="anchor" id="footnotes"></a>Footnotes</h3>
<p>Here is a simple footnote<sup class="footnote-ref"><a href="#fn-1" id="fnref-1" data-footnote-ref>1</a></sup>. With some<sup class="footnote-ref"><a href="#fn-2" id="fnref-2" data-footnote-ref>2</a></sup> additional text after it.</p>
<p>A reference<a href="https://en.wikipedia.org/wiki/Hobbit#Lifestyle" title="Hobbit lifestyles">1</a> can also be used to just create a link hidden from footnotes.</p>
<pre><code class="language-markdown">Here is a simple footnote[^1]. With some[^2] additional text after it.  

A reference[1] can also be used to just create a link hidden from footnotes.
</code></pre>
<p>And on the end of the file:</p>
<pre><code class="language-markdown">[^1]: My footnote.
[^2]: Another footnote.
[1]: &lt;https://en.wikipedia.org/wiki/Hobbit#Lifestyle&gt; &quot;Hobbit lifestyles&quot;
</code></pre>
<blockquote>
<p>[!TIP]<br />
Add global references to the <code>_references.md</code> file to reuse on any content.</p>
</blockquote>
<h3><a href="#block-quote" aria-hidden="true" class="anchor" id="block-quote"></a>Block quote</h3>
<p>&gt;Not a quote</p>
<blockquote>
<p>quote</p>
<blockquote>
<blockquote>
<p>Nested quote</p>
</blockquote>
</blockquote>
</blockquote>
<pre><code class="language-markdown">&gt;Not a quote
&gt; quote
&gt; &gt; &gt; Nested quote
</code></pre>
<p>Multiline quote</p>
<blockquote>
<p>&quot;Marmite is the easiest SSG&quot; created by
Bruno Rocha with the contribution of various people.</p>
</blockquote>
<pre><code class="language-markdown">&gt;&gt;&gt;
&quot;Marmite is the easiest SSG&quot; created by
Bruno Rocha with the contribution of various people.
&gt;&gt;&gt;
</code></pre>
<p>Multi paragraph quote</p>
<blockquote>
<p>Dorothy followed her through many of the beautiful rooms in her castle.</p>
<p>The Witch bade her clean the pots and kettles and sweep the floor and keep the fire fed with</p>
</blockquote>
<pre><code class="language-markdown">&gt; Dorothy followed her through many of the beautiful rooms in her castle.
&gt; 
&gt; The Witch bade her clean the pots and kettles and sweep the floor and keep the fire fed with 
</code></pre>
<p>Rich quotes</p>
<blockquote>
<p><strong>The quarterly results look great!</strong></p>
<ul>
<li>Revenue was off the chart.</li>
<li>Profits were higher than ever.</li>
</ul>
<p><em>Everything</em> is going according to <strong>plan</strong>.</p>
</blockquote>
<pre><code class="language-markdown">&gt; **The quarterly results look great!**
&gt; 
&gt; - Revenue was off the chart.
&gt; - Profits were higher than ever.
&gt; 
&gt;  *Everything* is going according to **plan**.
</code></pre>
<h3><a href="#underline" aria-hidden="true" class="anchor" id="underline"></a>Underline</h3>
<p><u>dunder</u></p>
<pre><code class="language-markdown">__dunder__
</code></pre>
<h3><a href="#code" aria-hidden="true" class="anchor" id="code"></a>Code</h3>
<pre><code class="language-python">import antigravity

def main():
    print(&quot;Python is a great language&quot;)
</code></pre>
<pre><code class="language-rust">fn main() {
    println!(&quot;Marmite is made with Rust!&quot;);
}
</code></pre>
<pre><code class="language-markdown">```python
import antigravity

def main():
    print(&quot;Python is a great language&quot;)
```

```rust
fn main() {
    println!(&quot;Marmite is made with Rust!&quot;);
}
```
</code></pre>
<h3><a href="#lists" aria-hidden="true" class="anchor" id="lists"></a>lists</h3>
<ul>
<li>lists
<ul>
<li>sub item</li>
</ul>
</li>
<li>images
<ul>
<li>other</li>
</ul>
</li>
<li>tables</li>
<li>Formatting</li>
</ul>
<pre><code class="language-markdown">- lists
  - sub item
- images
  * other
- tables
- Formatting
</code></pre>
<p>Numbered</p>
<ol>
<li>First item</li>
<li>Second item
<ul>
<li>Indented unordered item</li>
<li>Indented unordered item</li>
</ul>
</li>
<li>Third item
<ol>
<li>Indented ordered item</li>
<li>Indented ordered item</li>
</ol>
</li>
<li>Fourth item</li>
</ol>
<pre><code class="language-markdown">1. First item
1. Second item
    - Indented unordered item
    - Indented unordered item
1. Third item
    1. Indented ordered item
    1. Indented ordered item
1. Fourth item 
</code></pre>
<p>Starting lists with numbers requires a <code>number\</code></p>
<ul>
<li>1983. A great year!</li>
<li>I think 1984 was second best.</li>
</ul>
<pre><code class="language-markdown">- 1983\. A great year!
- I think 1984 was second best. 
</code></pre>
<h3><a href="#images" aria-hidden="true" class="anchor" id="images"></a>Images</h3>
<p>Photo<br />
<figure><img src="media/marmite.jpg" alt="Photo" /></figure></p>
<p>Same but containing a tooltip if you hover the mouse on<br />
<figure><img src="media/marmite.jpg" alt="Photo" title="A jar of Marmite" /><figcaption>A jar of Marmite</figcaption></figure></p>
<pre><code class="language-markdown">Photo  
![Photo](media/marmite.jpg)

Same but containing a tooltip if you hover the mouse on  
![Photo](media/marmite.jpg &quot;A jar of Marmite&quot;)
</code></pre>
<hr />
<h2><a href="#creating-links" aria-hidden="true" class="anchor" id="creating-links"></a>Creating Links</h2>
<h3><a href="#regular-link" aria-hidden="true" class="anchor" id="regular-link"></a>Regular link</h3>
<p><a href="https://github.com/rochacbruno/marmite">Marmite</a></p>
<p><a href="https://pudim.com.br" title="A picture of a pudim">A link with a tooltip</a></p>
<pre><code class="language-markdown">[Marmite](https://github.com/rochacbruno/marmite)

[A link with a tooltip](https://pudim.com.br &quot;A picture of a pudim&quot;)  
</code></pre>
<h3><a href="#auto-link" aria-hidden="true" class="anchor" id="auto-link"></a>Auto-link</h3>
<p><a href="https://github.com/rochacbruno/marmite">https://github.com/rochacbruno/marmite</a><br />
<a href="https://www.markdownguide.org">https://www.markdownguide.org</a><br />
<a href="mailto:fake@example.com">fake@example.com</a></p>
<pre><code class="language-markdown">https://github.com/rochacbruno/marmite  
&lt;https://www.markdownguide.org&gt;  
&lt;fake@example.com&gt;
</code></pre>
<h3><a href="#wikilinks" aria-hidden="true" class="anchor" id="wikilinks"></a>Wikilinks</h3>
<p>Wikilinks allows to link using <code>[[name|url]]</code> syntax.</p>
<p><a href="getting-started.html" data-wikilink="true">Read the Tutorial</a> and <a href="getting-started.html" data-wikilink="true">Read the Tutorial</a> and <a href="getting-started.html" data-wikilink="true">Read the Tutorial</a></p>
<p>Anchors also supported <a href="about.html#faq" data-wikilink="true">FAQ</a></p>
<p><a href="https://pudim.com.br" data-wikilink="true">Pudim</a></p>
<p>It also resolve anchors like <a href="#wikilinks" data-wikilink="true">Wikilinks</a><br />
and internal content by title like
<a href="markdown-format.html#wikilinks" data-wikilink="true">MD Wikilinks</a> or <a href="markdown-format.html" data-wikilink="true">MD Format</a></p>
<pre><code class="language-markdown">[[Read the Tutorial|getting-started]] and [[Read the Tutorial|getting-started.md]] and [[Read the Tutorial|getting-started.html]]

[[Pudim|https://pudim.com.br]]

It also resolve anchors like [[Wikilinks|#wikilinks]]  
and internal content by title like 
[[MD Wikilinks|Markdown Format#Wikilinks]] or [[MD Format|Markdown Format]]

</code></pre>
<h3><a href="#obsidian-links" aria-hidden="true" class="anchor" id="obsidian-links"></a>Obsidian Links</h3>
<p>Obsidian links are made using <code>[[page-slug]]</code> or <code>[[page-slug.md]]</code></p>
<p>Example:</p>
<p><a href="about.html" data-wikilink="true">about</a> and <a href="about.html" data-wikilink="true">about</a> and <a href="about.html" data-wikilink="true">about</a> should point both to the about page.</p>
<p>Anchors also supported <a href="about.html#faq" data-wikilink="true">about > faq</a></p>
<p><a href="https://pudim.com.br" data-wikilink="true">https://pudim.com.br</a></p>
<p>It also resolve anchors like <a href="#obsidian-links" data-wikilink="true">Obsidian Links</a><br />
and internal content by title like<br />
<a href="markdown-format.html#obsidian-links" data-wikilink="true">Markdown Format > Obsidian Links</a> or <a href="markdown-format.html" data-wikilink="true">Markdown Format</a></p>
<pre><code class="language-markdown">[[about]] and [[about.md]] and [[about.html]] should point both to the about page.

Anchors also supported [[about#faq]]

[[https://pudim.com.br]]

It also resolve anchors like [[#Obsidian Links]]  
and internal content by title like 
[[Markdown Format#Obsidian Links]] or [[Markdown Format]]

</code></pre>
<h3><a href="#back-links" aria-hidden="true" class="anchor" id="back-links"></a>Back-links</h3>
<p>Every time you link to another page or post
using the backreference like <code>{slug}</code>, <code>{slug}.md</code> or <code>{slug}.html</code>
<strong>marmite</strong> will track the backlinking and show
a list of pages that links to each other.</p>
<p>Examples:</p>
<p><a href="hello.html" data-wikilink="true">hello</a>
<a href="hello.html">Hello1</a>
<a href="hello.html">Hello2</a>
<a href="hello.html" data-wikilink="true">Hello3</a>
<a href="hello.html" data-wikilink="true">Hello4</a>
<a href="hello.html">Hello5</a></p>
<pre><code class="language-markdown">[[hello]]
[Hello1](hello.html)
[Hello2](hello.md)
[[Hello3|hello]]
[[Hello4|hello.md]]
&lt;a href=&quot;hello.html&quot;&gt;Hello5&lt;/a&gt;
</code></pre>
<p>In any case the <code>hello.html</code> page will have a this page
on its list of back-links:</p>
<figure>
  <figcaption>Backlinks</figcaption>
  <img src="media/screenshots/backlink.png" width="500">
</figure>
<hr />
<h2><a href="#extensions" aria-hidden="true" class="anchor" id="extensions"></a>Extensions</h2>
<h3><a href="#task" aria-hidden="true" class="anchor" id="task"></a>Task</h3>
<ul>
<li><input type="checkbox" checked="" disabled="" /> Task 1</li>
<li><input type="checkbox" disabled="" /> Task 2</li>
</ul>
<pre><code class="language-markdown">- [x] Task 1
- [ ] Task 2
</code></pre>
<h3><a href="#emoji" aria-hidden="true" class="anchor" id="emoji"></a>Emoji</h3>
<p>😄 - 🦀 - 🐍</p>
<pre><code class="language-markdown">:smile: - :crab: - :snake:
</code></pre>
<h3><a href="#description-lists" aria-hidden="true" class="anchor" id="description-lists"></a>Description lists</h3>
<dl>
<dt>First term</dt>
<dd>Details for the <strong>first term</strong></dd>
<dt>Second term</dt>
<dd>Details for the <strong>second term</strong></dd>
<dd>More details in second paragraph.</dd>
</dl>
<pre><code class="language-markdown">First term
: Details for the **first term**

Second term
: Details for the **second term**
: More details in second paragraph.
</code></pre>
<h3><a href="#spoiler" aria-hidden="true" class="anchor" id="spoiler"></a>Spoiler</h3>
<p>This is <span class="spoiler">secret</span></p>
<pre><code class="language-markdown">This is ||secret||
</code></pre>
<h3><a href="#math" aria-hidden="true" class="anchor" id="math"></a>Math</h3>
<blockquote>
<p>Depends on <code>extra: {&quot;math&quot;: true}</code> defined on frontmatter, then <strong>MathJax</strong> is loaded.</p>
</blockquote>
<p>When $a \ne 0$, there are two solutions to \(ax^2 + bx + c = 0\) and they are
$$x = {-b \pm \sqrt{b^2-4ac} \over 2a}.$$</p>
<p>Inline math $1 + 2$ and display math $$x + y$$</p>
<p>$$
x^2
$$</p>
<pre><code class="language-html">When $a \ne 0$, there are two solutions to \\(ax^2 + bx + c = 0\\) and they are
$$x = {-b \pm \sqrt{b^2-4ac} \over 2a}.$$

Inline math $1 + 2$ and display math $$x + y$$

$$
x^2
$$
</code></pre>
<h3><a href="#alerts" aria-hidden="true" class="anchor" id="alerts"></a>Alerts</h3>
<p>Github Flavored Markdown (GFM) supports alerts, also called callouts or admonitions.</p>
<blockquote>
<p>[!NOTE]<br />
Highlights information that users should take into account, even when skimming.</p>
</blockquote>
<blockquote>
<p>[!TIP]
Optional information to help a user be more successful.</p>
</blockquote>
<blockquote>
<p>[!IMPORTANT]<br />
Crucial information necessary for users to succeed.</p>
</blockquote>
<blockquote>
<p>[!WARNING]<br />
Critical content demanding immediate user attention due to potential risks.</p>
</blockquote>
<blockquote>
<p>[!CAUTION]
Negative potential consequences of an action.</p>
</blockquote>
<pre><code class="language-markdown">&gt; [!NOTE]  
&gt; Highlights information that users should take into account, even when skimming.

&gt; [!TIP]
&gt; Optional information to help a user be more successful.

&gt; [!IMPORTANT]  
&gt; Crucial information necessary for users to succeed.

&gt; [!WARNING]  
&gt; Critical content demanding immediate user attention due to potential risks.

&gt; [!CAUTION]
&gt; Negative potential consequences of an action.
</code></pre>
<p>Optionally a title can be added to the alert:</p>
<blockquote>
<p>[!NOTE] For your attention
Highlights information that users should take into account, even when skimming.</p>
</blockquote>
<pre><code class="language-markdown">&gt; [!NOTE] For your attention
&gt; Highlights information that users should take into account, even when skimming.
</code></pre>
<h3><a href="#diagrams" aria-hidden="true" class="anchor" id="diagrams"></a>Diagrams</h3>
<blockquote>
<p>Depends on <code>extra: {&quot;mermaid&quot;: true}</code> defined on frontmatter, then <strong>MermaidJS</strong> is loaded.</p>
<p><code>mermaid_theme</code> is also configurable with values <code>forest</code>,<code>neutral</code>,<code>dark</code>,<code>forest</code>,<code>base</code>,<code>default</code></p>
</blockquote>
<pre><code class="language-mermaid">sequenceDiagram
    Alice -&gt;&gt; Bob: Hello Bob, how are you?
    Bob--&gt;&gt;John: How about you John?
    Bob--x Alice: I am good thanks!
    Bob-x John: I am good thanks!
    Note right of John: Bob thinks a long&lt;br/&gt;long time, so long&lt;br/&gt;that the text does&lt;br/&gt;not fit on a row.

    Bob--&gt;Alice: Checking with John...
    Alice-&gt;John: Yes... John, how are you?

</code></pre>
<pre><code class="language-mermaid">graph LR
    A[Square Rect] -- Link text --&gt; B((Circle))
    A --&gt; C(Round Rect)
    B --&gt; D{Rhombus}
    C --&gt; D

</code></pre>
<pre><code class="language-mermaid">xychart-beta
    title &quot;Sales Revenue&quot;
    x-axis [jan, feb, mar, apr, may, jun, jul, aug, sep, oct, nov, dec]
    y-axis &quot;Revenue (in $)&quot; 4000 --&gt; 11000
    bar [5000, 6000, 7500, 8200, 9500, 10500, 11000, 10200, 9200, 8500, 7000, 6000]
    line [5000, 6000, 7500, 8200, 9500, 10500, 11000, 10200, 9200, 8500, 7000, 6000]

</code></pre>
<pre><code class="language-mermaid">pie title Pets adopted by volunteers
    &quot;Dogs&quot; : 386
    &quot;Cats&quot; : 85
    &quot;Rats&quot; : 15

</code></pre>
<pre><code class="language-mermaid">timeline
    title History of marmite
    2024-10-13 : Created
    2024-10-14 : First Release
               : First Contribution
    2024-10-20 : Big refactor
    2024-10-22 : Diagram support
</code></pre>
<details>
<summary> Click to see the raw mermaid </summary>
<pre><code class="language-markdown">```mermaid
sequenceDiagram
    Alice -&gt;&gt; Bob: Hello Bob, how are you?
    Bob--&gt;&gt;John: How about you John?
    Bob--x Alice: I am good thanks!
    Bob-x John: I am good thanks!
    Note right of John: Bob thinks a long&lt;br/&gt;long time, so long&lt;br/&gt;that the text does&lt;br/&gt;not fit on a row.

    Bob--&gt;Alice: Checking with John...
    Alice-&gt;John: Yes... John, how are you?

```

```mermaid
graph LR
    A[Square Rect] -- Link text --&gt; B((Circle))
    A --&gt; C(Round Rect)
    B --&gt; D{Rhombus}
    C --&gt; D

```

```mermaid
xychart-beta
    title &quot;Sales Revenue&quot;
    x-axis [jan, feb, mar, apr, may, jun, jul, aug, sep, oct, nov, dec]
    y-axis &quot;Revenue (in $)&quot; 4000 --&gt; 11000
    bar [5000, 6000, 7500, 8200, 9500, 10500, 11000, 10200, 9200, 8500, 7000, 6000]
    line [5000, 6000, 7500, 8200, 9500, 10500, 11000, 10200, 9200, 8500, 7000, 6000]

```

```mermaid
pie title Pets adopted by volunteers
    &quot;Dogs&quot; : 386
    &quot;Cats&quot; : 85
    &quot;Rats&quot; : 15

```

```mermaid
timeline
    title History of marmite
    2024-10-13 : Created
    2024-10-14 : First Release
               : First Contribution
    2024-10-20 : Big refactor
    2024-10-22 : Diagram support
```
</code></pre>
</details>
<hr />
<h2><a href="#html" aria-hidden="true" class="anchor" id="html"></a>HTML</h2>
<blockquote>
<p>All <strong>raw</strong> html is allowed</p>
</blockquote>
<h3><a href="#superscript" aria-hidden="true" class="anchor" id="superscript"></a>Superscript</h3>
<p>80<sup>2</sup></p>
<pre><code class="language-html">80&lt;sup&gt;2&lt;/sup&gt;
</code></pre>
<h3><a href="#embed" aria-hidden="true" class="anchor" id="embed"></a>Embed</h3>
<p>Just use raw HTML for now, in future we may have a shorcode.</p>
<iframe width="260" height="160" src="https://www.youtube.com/embed/MjrBTcnnK6c?si=PmQWsGiTh5XguSpb" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" referrerpolicy="strict-origin-when-cross-origin" allowfullscreen></iframe>
<pre><code class="language-html">&lt;iframe width=&quot;260&quot; height=&quot;160&quot; src=&quot;https://www.youtube.com/embed/MjrBTcnnK6c?si=PmQWsGiTh5XguSpb&quot; title=&quot;YouTube video player&quot;&gt;&lt;/iframe&gt;
</code></pre>
<h3><a href="#pico-css-components" aria-hidden="true" class="anchor" id="pico-css-components"></a>Pico CSS components</h3>
<p><small>The default embedded template uses <a href="https://picocss.com">picocss</a> so it is possible to write raw HTML like this:</small></p>
<h4><a href="#faq" aria-hidden="true" class="anchor" id="faq"></a>FAQ</h4>
<details>
<summary>Why is it named Marmite?</summary>
<p>The creator of this project was looking for some cool name
to use for a <strong>mark</strong>down related project.
Then while having bread with Marmite spread for breakfast
it looked like a good idea!</p>
</details>
<hr />
<details>
<summary>Why Rust?</summary>
<p><strong>Why not?</strong></p>
</details>
<hr />
<pre><code class="language-html">&lt;details&gt;
&lt;summary&gt;Why is it named Marmite?&lt;/summary&gt;

The ...

&lt;/details&gt;

&lt;hr /&gt;

&lt;details&gt;
&lt;summary&gt;Why Rust?&lt;/summary&gt;

**Why not?**

&lt;/details&gt;

&lt;hr /&gt;

</code></pre>
<h3><a href="#symbols" aria-hidden="true" class="anchor" id="symbols"></a>Symbols</h3>
<pre><code>Copyright (©) — &amp;copy;
Registered trademark (®) — &amp;reg;
Trademark (™) — &amp;trade;
Euro (€) — &amp;euro;
Left arrow (←) — &amp;larr;
Up arrow (↑) — &amp;uarr;
Right arrow (→) — &amp;rarr;
Down arrow (↓) — &amp;darr;
Degree (°) — &amp;#176;
Pi (π) — &amp;#960;
</code></pre>
<p>This is a ©left material.</p>
<pre><code class="language-markdown">This is a &amp;copy;left material.
</code></pre>
<p>Bye!</p>
<blockquote>
<p>Please consider giving a ☆ on Marmite <a href="https://github.com/rochacbruno/marmite">Github</a> repository, that helps a lot!</p>
</blockquote>
<!-- Contents from _markdown_footer.md are appended to every content -->
<!-- _references.md is a file to gather all references together 
this file is appended to every raw markdown.
-->
<section class="footnotes" data-footnotes>
<ol>
<li id="fn-1">
<p>My footnote. <a href="#fnref-1" class="footnote-backref" data-footnote-backref data-footnote-backref-idx="1" aria-label="Back to reference 1">↩</a></p>
</li>
<li id="fn-2">
<p>Another footnote <a href="#fnref-2" class="footnote-backref" data-footnote-backref data-footnote-backref-idx="2" aria-label="Back to reference 2">↩</a></p>
</li>
</ol>
</section>
]]></content:encoded></item><item><title>Hosting</title><link>https://www.myblog.com/blog//hosting.html</link><author>rochacbruno</author><category>docs</category><category>hosting</category><guid>https://www.myblog.com/blog//hosting.html</guid><pubDate>Wed, 16 Oct 2024 00:00:00 GMT</pubDate><source url="https://www.myblog.com/blog/">index</source><content:encoded><![CDATA[<p>Marmite genetates a static site, so you can host it in any web server.</p>
<p>Examples:</p>
<ul>
<li>Github pages</li>
<li>Gitlab pages</li>
<li>Netlify</li>
<li>Vercel</li>
<li>Nginx</li>
<li>Apache</li>
</ul>
<h3><a href="#github-pages" aria-hidden="true" class="anchor" id="github-pages"></a>Github Pages</h3>
<p>This is the easiest and cheapest option to publish your static blog,
you need a <strong>Github Repository</strong> containing a <code>content</code> directory and a <code>marmite.yaml</code></p>
<p>Use this template repository <a href="https://github.com/rochacbruno/blog">https://github.com/rochacbruno/blog</a> and give a meaninful name like <code>blog</code>.</p>
<p>Or start from scratch! it is very simple!</p>
<hr />
<p>Create a new repository and the following files.</p>
<pre><code class="language-bash">.
|_ marmite.yaml
|_ content
  |_ 2024-10-22-my-first-post.md
  |_ about.md
</code></pre>
<p>Edit <code>marmite.yaml</code> to add your preferences.</p>
<pre><code class="language-yaml">name: My Blog
tagline: Poems, Essays and Articles
url: https://YOURUSER.github.io/REPONAME
menu:
  - [&quot;About&quot;, &quot;about.html&quot;]
  - [&quot;Pages&quot;, &quot;pages.html&quot;]
  - [&quot;Tags&quot;, &quot;tags.html&quot;]
  - [&quot;Follow me&quot;, &quot;https://mastodon.social/@YOURUSER&quot;]
  - [&quot;Github&quot;, &quot;https://github.com/rochacbruno&quot;]
</code></pre>
<blockquote>
<p>[!TIP]<br />
you can use <code>--generate-config</code> to generate a default configuration file.</p>
</blockquote>
<p>Now you need to tell github actions to go inside your repo root and run</p>
<pre><code>marmite . site
</code></pre>
<p>And then publish the <code>site/</code> directory as the github page for your repo.</p>
<p>You can automate that:</p>
<p>First access <a href="https://github.com/YOURUSER/REPONAME/settings/pages">https://github.com/YOURUSER/REPONAME/settings/pages</a> and set the
pages source to <strong>Github Actions</strong></p>
<p>Then add a workflow to your repository.</p>
<p><code>.github/workflows/main.yaml</code></p>
<pre><code class="language-yaml">name: GH Pages Deploy

on:
  push:
    branches: [main]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: &quot;pages&quot;
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎
        uses: actions/checkout@v4

      - name: Install marmite 🫙
        run: cargo install marmite

      - name: Build site 🏗️
        run: marmite . site --debug

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'site'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
</code></pre>
<p>Now commit and push to the main branch and wait for your blog to be published at</p>
<p><a href="https://YOURUSER.github.io/REPONAME">https://YOURUSER.github.io/REPONAME</a></p>
<p>Read <a href="./customizing-templates.html">Customizing Templates</a> to learn how
to customize the look and feel of your blog.</p>
<blockquote>
<p>Please consider giving a ☆ on Marmite <a href="https://github.com/rochacbruno/marmite">Github</a> repository, that helps a lot!</p>
</blockquote>
<!-- Contents from _markdown_footer.md are appended to every content -->
<!-- _references.md is a file to gather all references together 
this file is appended to every raw markdown.
-->
]]></content:encoded></item><item><title>Enabling Comments</title><link>https://www.myblog.com/blog//enabling-comments.html</link><author>rochacbruno</author><category>docs</category><category>comments</category><category>customization</category><guid>https://www.myblog.com/blog//enabling-comments.html</guid><pubDate>Tue, 15 Oct 2024 00:00:00 GMT</pubDate><source url="https://www.myblog.com/blog/">index</source><content:encoded><![CDATA[<p>Marmite as a static site generator, doesn't have commenting features,
but there are various options of external commenting systems to integrate.</p>
<dl>
<dt>Utterances</dt>
<dd>
<p>Use github issues as comment system<br />
Requires user to login to github</p>
</dd>
<dt><strong>Giscus</strong></dt>
<dd>
<p>Use Github discussions as comment system<br />
requires user to login to github.</p>
</dd>
<dt>Hatsu</dt>
<dd>
<p>Federated to ActivityPub<br />
Requires a running instance<br />
Requires user to comment from a fediverse account.</p>
</dd>
</dl>
<p>Paid alternatives are Disqus and Commento, easy to add but not covered on this article.</p>
<hr />
<p>For now the easiest systems are Giscus and Utterances.</p>
<h2><a href="#setting-up-utterances" aria-hidden="true" class="anchor" id="setting-up-utterances"></a>Setting up Utterances</h2>
<ol>
<li>First you need to have a github repository
<ul>
<li>if you used the starting <a href="https://github.com/rochacbruno/make-me-a-blog">repo</a> you already got one.</li>
<li>You can create a repository solely for comments, the blog doesn't need to be hosted on the same repo.</li>
</ul>
</li>
<li>The repository must be public, otherwise visitors will not be able to view the discussion.</li>
<li>Install <a href="https://github.com/apps/utterances">utterances app</a> on your repo, otherwise visitors will not be able to comment and react.</li>
</ol>
<p>Add a <code>_comments.md</code> to your content folder.</p>
<pre><code class="language-markdown">##### Comments

&lt;script src='https://utteranc.es/client.js'
repo='youruser/your-repo'
issue-term='pathname'
theme='preferred-color-scheme'
crossorigin='anonymous'
async&gt;
&lt;/script&gt;
</code></pre>
<h2><a href="#setting-up-giscus" aria-hidden="true" class="anchor" id="setting-up-giscus"></a>Setting up Giscus</h2>
<ol>
<li>First you need to have a github repository
<ul>
<li>if you used the starting <a href="https://github.com/rochacbruno/make-me-a-blog">repo</a> you already got one.</li>
<li>You can create a repository solely for comments, the blog doesn't need to be hosted on the same repo.</li>
</ul>
</li>
<li>The repository must be public, otherwise visitors will not be able to view the discussion.</li>
<li>Install <a href="https://github.com/apps/giscus">giscus app</a> on your repo, otherwise visitors will not be able to comment and react.</li>
<li>The Discussions feature must be <a href="https://docs.github.com/en/github/administering-a-repository/managing-repository-settings/enabling-or-disabling-github-discussions-for-a-repository">enabled</a> on your repository.</li>
</ol>
<p>Now go to <a href="https://giscus.app/">https://giscus.app/</a> and find the <strong>configuration</strong> section, configure ir and and Copy the <code>&lt;script ... /script&gt;</code>.</p>
<p>Now there are 2 ways to add it to Marmite</p>
<ol>
<li>On a markdown fragment</li>
</ol>
<p>Add a <code>_comments.md</code> to your content folder.</p>
<pre><code class="language-markdown">##### Comments

&lt;script src=&quot;https://giscus.app/client.js&quot;
        data-repo=&quot;rochacbruno/marmite&quot;
        data-repo-id=&quot;R_kgDONAKMvQ&quot;
        data-category=&quot;Comments&quot;
        data-category-id=&quot;DIC_kwDONAKMvc4CjmH_&quot;
        data-mapping=&quot;pathname&quot;
        data-strict=&quot;0&quot;
        data-reactions-enabled=&quot;1&quot;
        data-emit-metadata=&quot;0&quot;
        data-input-position=&quot;bottom&quot;
        data-theme=&quot;preferred_color_scheme&quot;
        data-lang=&quot;en&quot;
        data-loading=&quot;lazy&quot;
        crossorigin=&quot;anonymous&quot;
        async&gt;
&lt;/script&gt;
</code></pre>
<h2><a href="#setting-up-other-comment-systems" aria-hidden="true" class="anchor" id="setting-up-other-comment-systems"></a>Setting up other comment systems</h2>
<p>The process will be very similar, you just need to grab the required <code>script</code> and tags.</p>
<p><code>content/_comments.md</code></p>
<pre><code class="language-markdown">##### Comments

&lt;div id=&quot;comment-system&quot;&gt;&lt;/div&gt;
&lt;script src=&quot;https://commentsystem.app/foo/bar/dothethings.js&quot;&gt;&lt;/script&gt;
</code></pre>
<h2><a href="#setting-on-the-config-file" aria-hidden="true" class="anchor" id="setting-on-the-config-file"></a>Setting on the config file</h2>
<ol start="2">
<li>Alternatively, add to  <code>marmite.yaml</code> extra section.</li>
</ol>
<pre><code class="language-yaml">extra:
  comments:
    title: Comments
    source: |
        &lt;script src=&quot;https://giscus.app/client.js&quot;
        data-repo=&quot;yourrepo/blog&quot;
        data-repo-id=&quot;sdsdsd&quot;
        data-category=&quot;Announcements&quot;
        data-category-id=&quot;dfsffsdfsdfsdfsdf&quot;
        data-mapping=&quot;pathname&quot;
        data-strict=&quot;0&quot;
        data-reactions-enabled=&quot;1&quot;
        data-emit-metadata=&quot;0&quot;
        data-input-position=&quot;top&quot;
        data-theme=&quot;preferred_color_scheme&quot;
        data-lang=&quot;en&quot;
        data-loading=&quot;lazy&quot;
        crossorigin=&quot;anonymous&quot;
        async&gt;
        &lt;/script&gt;
</code></pre>
<p>Same for any other comment system</p>
<pre><code class="language-yaml">extra:
  comments:
    title: Comments
    source: |
        &lt;div id=&quot;comment-system&quot;&gt;&lt;/div&gt;
        &lt;script src=&quot;https://commentsystem.app/foo/bar/dothethings.js&quot;&gt;&lt;/script&gt;
</code></pre>
<h2><a href="#customizing-the-html-template-directly" aria-hidden="true" class="anchor" id="customizing-the-html-template-directly"></a>Customizing the HTML template directly</h2>
<p>Add <code>templates/comments.html</code> to your project.</p>
<pre><code class="language-html">&lt;article&gt;
&lt;header&gt;{{site.extra.comments.title | default(value=&quot;Comments&quot;) }}&lt;/header&gt;
{{site.extra.comments.source}}
&lt;/article&gt;
</code></pre>
<blockquote>
<p>Please consider giving a ☆ on Marmite <a href="https://github.com/rochacbruno/marmite">Github</a> repository, that helps a lot!</p>
</blockquote>
<!-- Contents from _markdown_footer.md are appended to every content -->
<!-- _references.md is a file to gather all references together 
this file is appended to every raw markdown.
-->
]]></content:encoded></item><item><title>Pagination</title><link>https://www.myblog.com/blog//pagination.html</link><author>rochacbruno</author><category>docs</category><guid>https://www.myblog.com/blog//pagination.html</guid><pubDate>Sun, 01 Jan 2023 00:00:00 GMT</pubDate><source url="https://www.myblog.com/blog/">index</source><content:encoded><![CDATA[<!-- This page uses JSON as front-matter, this format is support but not recommended, use YAML `---` or TOML `+++` -->
<p>Yes! we have pagination!</p>
<p>Set <code>pagination: 10</code> on your <code>marmite.yaml</code></p>
<p>The pagination template is very simple</p>
<pre><code class="language-html">&lt;div class=&quot;pagination&quot;&gt;
    &lt;nav&gt;
        &lt;ul&gt;
        {% if previous_page %}
            &lt;li&gt;&lt;a href=&quot;{{previous_page}}&quot;&gt;&lt;strong&gt;&amp;larr;&lt;/strong&gt;&lt;/a&gt;&lt;/li&gt;
        {% endif %}
        &lt;/ul&gt;
        &lt;ul&gt;
            &lt;li&gt;&lt;small&gt;{{current_page_number}}/{{total_pages}}&lt;/small&gt;&lt;/li&gt;
        &lt;/ul&gt;
        &lt;ul&gt;
        {% if next_page %}
            &lt;li&gt;&lt;a href=&quot;{{next_page}}&quot;&gt;&lt;strong&gt;&amp;rarr;&lt;/strong&gt;&lt;/a&gt;&lt;/li&gt;
        {% endif %}
        &lt;/ul&gt;
    &lt;/nav&gt;
&lt;/div&gt;
</code></pre>
<blockquote>
<p>Please consider giving a ☆ on Marmite <a href="https://github.com/rochacbruno/marmite">Github</a> repository, that helps a lot!</p>
</blockquote>
<!-- Contents from _markdown_footer.md are appended to every content -->
<!-- _references.md is a file to gather all references together 
this file is appended to every raw markdown.
-->
]]></content:encoded></item></channel></rss>